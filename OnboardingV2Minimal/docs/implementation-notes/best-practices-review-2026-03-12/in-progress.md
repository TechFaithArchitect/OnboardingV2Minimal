# Best-Practices Review In-Progress Tracker (2026-03-12)

Active work items extracted from the source plan.

## A. Apex (Architecture, Security, Limits, Testability)
- In progress: `RecordCollectionEditorGetRecordsService` and `ExperienceOpportunityCreateAsyncService` `WITH SECURITY_ENFORCED` hardening on targeted dynamic/lookup query paths is now deployed and verified in org; remaining SOQL parity work is broader codebase follow-up outside this focused pass.
- In progress: `RecordCollectionEditorConfigService` metadata-driven record-editor inserts/updates (including relationship-record DML) are now deployed in `AccessLevel.USER_MODE`; relationship-field write gating (`isCreateable`/`isUpdateable`), relationship-lookup `WITH SECURITY_ENFORCED`, and editor-config inaccessible-field filtering are now in place.
- In progress: `RecordCollectionEditorGetRecordsService` now safely handles sparse queried records (guards against unqueried-field access during JSON serialization), filters inaccessible configured field names before extraction/query, applies `WITH SECURITY_ENFORCED` on fallback relationship lookup queries, and returns user-safe reference-based error messages while logging detailed diagnostics.
- In progress: `RecordCollectionEditorAsyncService`, `PicklistHelper`, and `SalesforceMessagingProvider` now route runtime diagnostics through `LoggingUtil`; remaining raw `System.debug` usage is intentionally centralized inside `LoggingUtil` itself.
- In progress: ad hoc runtime debug logging cleanup has been extended across `EmailTemplateSyncService`, `EmailTemplateSyncOrchestrator`, `EmailTemplateSyncJob`, `RecordCollectionEditorAsyncService`, `FollowUpDetectionService`, `FollowUpExecutionService`, `FollowUpFatigueService`, `FollowUpMessagingService`, and `TwilioSMSProvider`; production-class `System.debug` usage now resolves only through `LoggingUtil`.
- In progress: shared email bulk service `CommunicationTemplateBulkSendAction` now applies explicit Apex email-invocation guardrails (`Limits.getLimitEmailInvocations - Limits.getEmailInvocations`) so oversized payloads are safely bounded rather than overrunning `Messaging.sendEmail` invocation limits.

## B. Triggers (Governance, Recursion, Tests)
- In progress: `EmailCommTerritoryRoleHelperTest` now covers clear-to-null cleanup, operation idempotency, and a 210-record mixed-state update path.

## C. LWC (LDS-First, Accessibility, Testing, Performance)
- In progress: `objectRelatedList` inline-edit picklist incident is stabilized in org using `getPicklistValuesByRecordType` queueing with telemetry and now has Jest regression coverage in `force-app/test/lwc/objectRelatedList.test.js` (`6/6` passing); remaining UI hardening is accessibility + performance review.
- In progress: cleanup/hardening wave on `objectRelatedList` is now covered by deploy-safe Jest tests outside metadata bundles (`force-app/test/lwc`), including dotted-path query config + record-type picklist fallback/queue behavior; remaining incremental hardening is UX/accessibility.
- In progress: `ObjectRelatedListController` now has deploy-validated dynamic SOQL guardrails (describe-driven object/field/path validation and safer lookup query assembly) plus dedicated Apex regression coverage (`ObjectRelatedListCtrlTest`); next UI-side hardening focus is accessibility and cache/performance tuning.
- In progress: parent relationship filter compatibility has been restored for `objectRelatedList` configs that rely on cross-object parent paths (for example `Parent.Onboarding__c` on `Onboarding_Requirement_History`) after validator tightening; regression is now locked by dedicated Apex test coverage.
- In progress: dotted relationship-path compatibility is now restored across all `objectRelatedList` query-field inputs (including `Fields (CSV)` values like `CreatedBy.Name`) with regression tests in place; remaining `objectRelatedList` hardening work is Jest/UI behavior coverage, not Apex query-contract compatibility.
- In progress: `objectRelatedList` Jest coverage includes dotted-path configuration plus picklist-by-recordType resilience cases (success hydration, wire-error fallback, multi-recordType queue progression) with suite `force-app/test/lwc/objectRelatedList.test.js` passing `6/6`; tests are in `force-app/test/lwc` to keep Salesforce deploys compile-safe.
- In progress: `programDatesRelatedList` and `programDatesQuickAction` now have DOM-driven Jest regression suites in `force-app/test/lwc` covering wire transforms/errors, inline edit save success/error, row delete action, quick-action submit defaults, and success/error toasts + close behavior (`11/11` targeted passing; full Jest baseline `17/17`).

## D. Flows (Fault Handling, Modularity, Governance)
- In progress: Repo flow metadata was re-baselined from org source-of-truth on 2026-03-19 using an intersection-only retrieve (`78` `Flow` + `4` `FlowDefinition` already present in repo); no new flow files were introduced.
- In progress: Fault-connector hardening is now complete for the active allowed scope (`DOMAIN_OmniSObject_*`, `EXP_*`, plus `BLL_*_Logical_Process` where applicable). Current `make audit-fault-connectors` output shows only 5 remaining no-fault entries, and all 5 are Salesforce platform-exempt before-save Fast Field Updates flows where `Update Records` cannot have fault connectors (`BLL_External_Contact_Credential_RCD_Prevent_Duplicates`, `BLL_External_Contact_Credential_Type_RCD_Prevent_Duplicates`, `BLL_Training_Assignment_Onboarding_RCD_Prevent_Duplicates`, `BLL_Training_Assignment_RCD_Populate_Unique_Assignment_Key`, `BLL_Vendor_Program_Training_Requirement_RCD_Prevent_Duplicates`).
- In progress: Deployment blocker remediation for `BLL_Training_Assignment_SCD_Training_Reminder_Emails` is now on the current pattern: replaced broken legacy Apex action reference (`01p...NotFound`) with new invocable `CommunicationTemplateBulkSendAction`, changed payload contract to bulk list inputs (`communicationTemplateIds`, `recipientIds`, `onboardingIds`), and retained standard email semantics (`TargetObjectId` contact logging, `WhatId` onboarding linkage, org-wide sender from configurable flow input); targeted dry-run + test validation succeeded (`0AfRL00000dRDkI0AW`) and deploy to `OnboardV2` succeeded (`0AfRL00000dRDdq0AG`).
- In progress: `DOMAIN_OmniSObject_SFL_CREATE_Onboarding_Requirements` now snapshots `Fulfillment_Policy_Key__c` and delegates runtime subject expansion to `DOMAIN_OmniSObject_SFL_CREATE_Onboarding_Requirement_Subjects`; first-pass subject models are `Account`, `AllContacts`, and `PrincipalOwner`, with contact-scoped subject sourcing intentionally driven from `AccountContactRelation`.
- In progress: `DOMAIN_OmniSObject_SFL_CREATE_Onboarding_Requirements` record-type resolution has been optimized to avoid per-item `RecordType` queries: the flow now loads all `Onboarding_Requirement__c` record types once before entering the `VendorProgramRequirements` loop, resolves matching IDs in-memory via looped developer-name comparison, and routes missing-mapping fail-fast through a single outside-loop decision/action path.
- In progress: loop-shape governance is now automated via `scripts/automation/audit-flow-loop-shape.sh` and `scripts/automation/audit-flow-loop-connectors.sh` (wired into `make` targets); latest run confirms `0` direct `nextValueConnector` paths into query/DML/action elements and `0` `nextValue -> subflow` review candidates after refactoring `BLL_Onboarding_SFL_Dispatch_Communication_By_Context` to preload dispatch policy outside the template loop.
- In progress: `BLL_Onboarding_SFL_Dispatch_Communication_By_Context` now batches recipient sends through one `CommunicationTemplateBulkSendAction` call per template (list inputs) instead of per-recipient `DOMAIN_OmniSObject_ACTION_Send_Communication_Template` calls inside `Loop_Recipient_Ids`, reducing action-in-loop pressure and aligning send behavior with shared limit-guarded bulk dispatch.
- In progress: hardening bundle (`BLL_Onboarding_SFL_Dispatch_Communication_By_Context` + `CommunicationTemplateBulkSendAction`) has been validated and deployed to `OnboardV2` (`0AfRL00000dRSmX0AW`) with `CommunicationTemplateBulkSendActionTest` passing (`4/4`); next flow hardening focus is additional large-flow resiliency/contract checks beyond fault-connector and loop-shape baseline audits.
- In progress: record-triggered entry-criteria tightening is now partially applied: `BLL_Order_RCD_Business_Logic` is set to `doesRequireRecordChangedToMeetCriteria=true`, but flows that already use `IsChanged` operators at start (`BLL_Agreement_RCD_Logical_Process`, `BLL_Onboarding_Requirement_Subject_RCD_Logical_Process`) must keep that pattern because Salesforce rejects combining `IsChanged` with `doesRequireRecordChangedToMeetCriteria=true`; validated by dry-run `0AfRL00000dRTCM0A4` and deployed `0AfRL00000dRN8Y0AW`.
- In progress: `BLL_External_Contact_Credential_RCD_Logical_Process` now has tightened update-entry gating (`Training_Sent_Date__c` null + `POE_Program__c = Verizon Fios D2D` + `POE_Process_Status__c = Training Sent`) with `doesRequireRecordChangedToMeetCriteria=true`, reducing repeated no-op interviews while preserving the training-send transition path; validated by dry-run `0AfRL00000dRV7h0AG` and deployed `0AfRL00000dRV9J0AW`.
- In progress: `BLL_Onboarding_Requirement_RCD_Logical_Process` performance hardening is now in place without splitting flows: trigger entry routes through `Is_Create_Event` before heavy retrieval work, create behavior remains unchanged through existing approval/related-record paths, and update normalization now uses `$Record.Onboarding__c` directly for evaluator input so non-create updates skip the create-only subflow retrieval chain; validated by dry-run `0AfRL00000dROpO0AW` and deployed `0AfRL00000dRVxJ0AW`.
- In progress: `BLL_Onboarding_RCD_Logical_Process` now short-circuits vendor-program retrieval on irrelevant updates: `Determine_Execution_Path` runs first, only create/communication-update branches assign `ExecutionPath` and invoke `Get_Vendor_Program`, and a new route decision dispatches post-lookup work (`Create` to requirement creation path, `CommunicationUpdate` to training/setup communication path); validated by dry-run `0AfRL00000dRWN70AO` and deployed `0AfRL00000dRQ9h0AG`.
- In progress: `BLL_OmniSObject_RCD_SYNC_Training_Assignments` now has an early no-op gate (`Should_Process_Training_Assignment_Change`) so only create and relevant update events (`Status__c` / `Contact__c` / `Onboarding__c` changed) continue into onboarding/evidence sync; irrelevant updates now exit before the heavy subflow chain. Validated by dry-run `0AfRL00000dRWbd0AG` and deployed `0AfRL00000dRWgT0AW`.
- In progress: `BLL_OmniSObject_RCD_SYNC_Training_Assignments` parent requirement re-evaluation path has been converted from looped per-id subflow execution to a single bulk Apex action (`OnbReqParentBulkEvalInvocable`), with fallback handling for record-type-restricted `Onboarding_Requirement__c.Status__c` writes so rollups can still converge using compatible status values or `Completed__c`-only updates when needed; deployed with passing specified tests (`0AfRL00000dRZsr0AG`, `5/5`).
- In progress: single-owner onboarding status model P0 was implemented and deployed (`0AfRL00000dRee20AC`) from plan `docs/implementation-notes/onboarding-status-single-owner-flow-plan-2026-03-19.md`: `OnboardingStatusEvaluatorInvocable` now bulk-processes all requests and supports `onboardingIds` collection input, `BLL_Order_RCD_Business_Logic` now routes status updates through evaluator action instead of direct field assignment, and parent rollup callers now use `OnbReqParentBulkEvalInvocable` bulk engine.
- In progress: owner-write governance is now automated for onboarding status via `scripts/automation/audit-onboarding-status-owner-writes.sh` and `make audit-onboarding-status-owner`; baseline run reports `0` direct writes outside `OnboardingStatusEvaluatorService`.
- In progress: `BLL_Agreement_RCD_Logical_Process` now applies subflow output-contract hardening for `Get_Onboarding_Record` and `Evaluate_Agreement_Subjects` (`errorMessage` output mapping + explicit failure decisions + shared fault-message handler); this is the supported path because record-triggered `FlowSubflow` metadata does not allow `faultConnector`.
- In progress: supported subflow failure contracts are now governed by `scripts/automation/audit-subflow-error-contracts.sh` (`make audit-subflow-error-contracts`), which checks that parent flows map `errorMessage` when called child flows expose it; current baseline reports `0` missing mappings.
- In progress: user-facing fault UX is now aligned on `EXP_Opportunity_SCR_Create_Record`: fault-handler branches map `DOMAIN_OmniSObject_SFL_CREATE_Fault_Message.UserFacingMessage` to screen error output, `ResolvedFlowErrorMessage` now prioritizes user-safe handler output, and admin logs retain detailed diagnostics (`ResolvedFaultMessage` + flow/element/context + interview reference).
- In progress: user-facing fault UX/logging parity is now expanded to `EXP_Contract_SCR_Send_Adobe_Agreement` with the same pattern (output-failure + fault-failure branches route through shared fault logger, screen paths consume `UserFacingMessage`, and display text surfaces the reference-bearing message).
- In progress: screen-flow fault UX contracts are now enforced in automation via `scripts/automation/audit-screen-flow-fault-user-messaging.sh`; current scope (`EXP_Opportunity_SCR_Create_Record`, `EXP_Contract_SCR_Send_Adobe_Agreement`) is compliant (`0` violations).
- In progress: global fault-handler context contracts are now enforced in automation via `scripts/automation/audit-fault-message-context-contract.sh` (`make audit-fault-message-context-contract`), covering all flow types that call `DOMAIN_OmniSObject_SFL_CREATE_Fault_Message` and excluding only the user-deferred `DOMAIN_OmniSObject_SFL_Send_Email_Communication`.
- In progress: deferred by user decision (2026-03-19) - leave `DOMAIN_OmniSObject_SFL_Send_Email_Communication` unchanged for now; keep it queued as a later hardening item.
- In progress: shared `Follow_Up_Queue__c` fault records now capture source flow API, source element API, source element label, context key / record id, onboarding id when available, running user id, and interview GUID context on active non-deferred handlers; flow-version logging remains follow-up debt.
- In progress: `BLL_Training_Assignment_SCD_Training_Reminder_Emails` now guards empty batch sends and resets payload state after each send/fault path to avoid duplicate carryover between training-assignment iterations.
- In progress: `DOMAIN_OmniSObject_SFL_CREATE_Onboarding_Requirement_Subjects` now logs missing fulfillment policies, unsupported subject models, and subject-row create faults into shared admin fault records, no longer performs fulfillment-policy `Get Records` inside the onboarding-requirement loop, bulk-fetches requirements via `DOMAIN_OmniSObject_SFL_GET_Onboarding_Requirement`, bulk-fetches policies via `DOMAIN_OmniSObject_SFL_GET_Onboarding_Fullfilment_Policy`, and reuses a single `DOMAIN_OmniSObject_SFL_GET_Account_Contact_Relationship` retrieval with in-memory `Principal Owner` filtering; `DOMAIN_OmniSObject_SFL_CREATE_Onboarding_Requirements` no longer marks completion on a nonblank subject-expansion error.
- In progress: `DOMAIN_OmniSObject_SFL_GET_Onboarding_Requirement_Subjects`, `DOMAIN_OmniSObject_SFL_EVAL_Onb_Req_Subjects_Parent`, and async `BLL_Onboarding_Requirement_Subject_RCD_Logical_Process` now provide the first-pass aggregator layer, using filtered collections plus `AssignCount` to map child subject statuses back to parent `Onboarding_Requirement__c.Status__c` / `Completed__c` when `Onboarding_Requirement_Subject__c.Status__c` changes; `DOMAIN_OmniSObject_SFL_EVAL_Onb_Req_Subjects_By_Evidence` plus active `BLL_OmniSObject_RCD_SYNC_Training_Assignments` now provide the first live evidence evaluator for `Training_Assignment__c`, and the training sync flow now explicitly invokes the parent evaluator after subject updates instead of relying only on the async subject-trigger side effect, while contract-backed compliance statuses, agreements, and credentials still remain.
- In progress: `DOMAIN_OmniSObject_SFL_CREATE_Onboarding_Requirement_Subjects` now demonstrates the target pattern by replacing repeated in-loop `Get Records` with reusable collection-based getter flows and in-memory filtering.
- In progress: `BLL_Onboarding_Requirement_Subject_RCD_Logical_Process` now limits entry to `Update` events where `Status__c` changed and uses `AsyncAfterCommit` to avoid noisy rollups during initial subject creation.
- In progress: business-facing CMDT operating guide for onboarding status is now in place at `docs/user-guides/onboarding-status-business-rules-guide.md`; next step is validating that future status/rule changes can be executed entirely through CMDT updates and not direct flow edits.

## E. Data Model, FLS, and Metadata Hygiene
- In progress: `Onboarding_Requirement_Subject__c`, `Onboarding_Fulfillment_Policy__mdt`, and `Fulfillment_Policy_Key__c` snapshots now separate requirement definition from runtime subject fulfillment, while preserving the parent `Onboarding_Requirement__c` business-rules-engine pattern.
- In progress: contact-scoped subject expansion now intentionally sources relationship role context from `AccountContactRelation`, not `Contact`, and stores that runtime role snapshot on `Onboarding_Requirement_Subject__c.Role_Snapshot__c`.
- In progress: `Onboarding_Program_Sales_Team` and `Onboarding_Account_Services` now carry the Record Collection Editor access through permission sets, and the source copies were normalized against the current `OnboardV2` metadata shape on 2026-03-13.

## Immediate Concrete Actions (Proposed Diffs)
- In progress: `VendorOnboardingService`, `VendorOnboardingEligibilityRuleEngine`, `VendorPrerequisiteEvaluator`, `TwilioSettingsController`, `TwilioSMSProvider`, `FollowUpDetectionService`, `FollowUpExecutionService`, `FollowUpFatigueService`, `FollowUpMessagingService`, `FollowUpRuleRepository`, `SalesforceMessagingProvider`, `RecordCollectionEditorConfigService`, `RecordCollectionEditorGetRecordsService`, `RecordCollectionEditorAsyncService`, `ExperienceOpportunityCreateAsyncService`, and `PicklistHelper` updated through 2026-03-13.
