# Best-Practices Review Execution Log (2026-03-12)

Chronological implementation and validation log.
## Execution Log
- 2026-03-12: Started `P0` step 1 (Apex sharing/FLS enforcement audit + fixes).
- 2026-03-12: Updated `VendorOnboardingService`, `VendorPrerequisiteEvaluator`, `TwilioSettingsController`, `FollowUpMessagingService`, and `TwilioSMSProvider` for stronger sharing/query security posture.
- 2026-03-12: Targeted dry-run deploy compile validation succeeded for the above class changes.
- 2026-03-12: Extended security hardening to `FollowUpDetectionService`, `FollowUpExecutionService`, `FollowUpFatigueService`, `FollowUpRuleRepository`, `SalesforceMessagingProvider`, and `VendorOnboardingEligibilityRuleEngine`.
- 2026-03-12: Additional targeted dry-run deploy compile validation succeeded for all newly updated classes.
- 2026-03-12: Focused org test run (`707RL00001HofRVYAZ`) completed with 2 failures in existing org code (`VendorOnboardingJsonAdapterTest.testGetVendorsAsJson`, `VendorOnboardingLWCAdapterTest.testGetVendorOptionsReturnsExpectedData`); local changes were dry-run only and not deployed during this pass.
- 2026-03-12: Optimized `EmailCommTerritoryRoleHelper` with recursion-safe processing, precomputed user lookup field detection, and guarded DML execution.
- 2026-03-12: Trigger/helper dry-run compile validation succeeded for `TerritoryAssignmentsTrigger` + `EmailCommTerritoryRoleHelper`.
- 2026-03-12: `VersioningTriggerHandler` was intentionally removed from this repo and the org; that retired path is out of scope for current remediation.
- 2026-03-12: Added `EmailCommTerritoryRoleHelperTest` coverage for trigger-created role assignments and operation-aware idempotency behavior.
- 2026-03-12: Territory helper + trigger + new test dry-run compile validation succeeded.
- 2026-03-12: Hardened `TerritoryAssignmentsTrigger` + `EmailCommTerritoryRoleHelper` to diff user-lookup fields on update, skip unnecessary resync work, and delete stale role assignments when lookups are cleared.
- 2026-03-12: Expanded `EmailCommTerritoryRoleHelperTest` with clear-to-null regression coverage and a 210-record mixed-state update scenario.
- 2026-03-12: Targeted dry-run validation with `RunSpecifiedTests` succeeded for territory trigger/helper/test updates (`0AfRL00000dJTiz0AG`).
- 2026-03-12: Replaced ad hoc `System.debug` usage in `EmailTemplateSyncService`, `EmailTemplateSyncOrchestrator`, and `EmailTemplateSyncJob` with standardized `LoggingUtil` calls.
- 2026-03-12: EmailTemplateSync logging cleanup dry-run compile validation succeeded.
- 2026-03-12: Added typed Flow facades (`OnboardingInvocables`, `OnboardingFollowUpInvocables`) with request/response DTOs for vendor eligibility JSON retrieval and follow-up evaluation operations.
- 2026-03-12: Added `OnboardingInvocablesTest` coverage for valid/invalid request handling across both new invocable actions.
- 2026-03-12: Replaced remaining ad hoc follow-up/twilio `System.debug` usage in `FollowUpDetectionService`, `FollowUpExecutionService`, `FollowUpMessagingService`, `FollowUpFatigueService`, and `TwilioSMSProvider` with `LoggingUtil`.
- 2026-03-12: Adjusted invocable implementation after compile validation: Apex supports one `@InvocableMethod` per class, so follow-up evaluation moved into `OnboardingFollowUpInvocables`.
- 2026-03-12: Targeted dry-run compile validation succeeded for invocable/logging updates (`0AfRL00000dJHYB0A4`).
- 2026-03-12: Targeted dry-run validation with `RunSpecifiedTests` succeeded for `OnboardingInvocablesTest` (`0AfRL00000dJAQH0A4`), including class-level coverage on both new invocable classes.
- 2026-03-12: Began `P0` step 2 (flow fault-handler wiring) with representative implementation on `DOMAIN_OmniSObject_ACTION_Send_Communication_Template`.
- 2026-03-12: Added fault-path routing from `Send_Email_Action` to queue-based fault logging (`DOMAIN_OmniSObject_SFL_CREATE_Follow_Up_Queue`) with guarded onboarding checks and standardized trigger-reason/message formulas.
- 2026-03-12: Flow metadata constraint observed: `faultConnector` is invalid on `FlowSubflow` elements; representative fault wiring must target elements that support fault connectors (e.g., `actionCalls`).
- 2026-03-12: Targeted dry-run compile validation succeeded for `DOMAIN_OmniSObject_ACTION_Send_Communication_Template` (`0AfRL00000dJNH00AO`).
- 2026-03-12: Added reusable fault-message subflow `DOMAIN_OmniSObject_SFL_CREATE_Fault_Message` to standardize communication-fault message normalization, trigger-reason generation, and conditional follow-up queue creation.
- 2026-03-12: Updated `DOMAIN_OmniSObject_ACTION_Send_Communication_Template` and `BLL_Onboarding_SFL_Dispatch_Communication_By_Context` to call `DOMAIN_OmniSObject_SFL_CREATE_Fault_Message` instead of duplicating queue-routing logic.
- 2026-03-13: Targeted dry-run validation succeeded for the flow standardization work plus trigger/helper updates (`0AfRL00000dJUOv0AO`).
- 2026-03-13: Deployed trigger/helper hardening plus standardized fault-message flow updates to `OnboardV2` (`0AfRL00000dJUbp0AG`); deploy success was confirmed via `sf project deploy report` after a CLI reporting bug on the initial deploy command output.
- 2026-03-13: Extended `DOMAIN_OmniSObject_SFL_CREATE_Follow_Up_Queue` and `DOMAIN_OmniSObject_SFL_CREATE_Fault_Message` so shared communication-fault handling can override initial queue status and create terminal fault records even when no onboarding context is available.
- 2026-03-13: Updated `BLL_Training_Assignment_SCD_Training_Reminder_Emails` to route `EmailCommBatchSendAction` faults through the shared fault handler, skip empty batch sends, and reset email payload state between loop iterations to prevent cross-record carryover.
- 2026-03-13: Targeted dry-run validation succeeded for the queue/fault/training-reminder flow bundle (`0AfRL00000dJVpd0AG`).
- 2026-03-13: Deployed updated queue/fault/training-reminder flows to `OnboardV2` (`0AfRL00000dJU3z0AG`).
- 2026-03-13: Hardened legacy `DOMAIN_OmniSObject_SFL_Send_Email_Communication` with shared fault routing so email-send failures no longer fall through to `Training_Sent_Date__c` updates.
- 2026-03-13: Updated legacy orchestrator `BLL_External_Contact_Credential_RCD_Execute_Supplemental_Onboarding_Requirements` in place to pass onboarding context into the email subflow and remove stale `DOMAIN_OmniSObject_SFL_GET_Vendor_Program` step mappings that no longer match the current subflow contract; this became the final in-place hardening pass before the later `BLL_External_Contact_Credential_RCD_Logical_Process` replacement.
- 2026-03-13: Replaced the legacy external-credential orchestrator in source with standard record-triggered autolaunched flow `BLL_External_Contact_Credential_RCD_Logical_Process`, preserving the ECC update, signer-gated onboarding lookup, SSO-email dispatch, and process-status completion behavior without orchestrator stages.
- 2026-03-13: Targeted dry-run validation succeeded for the legacy external-credential flow bundle (`0AfRL00000dJZzi0AG`).
- 2026-03-13: Deployed legacy external-credential flow hardening to `OnboardV2` (`0AfRL00000dJd410AC`).
- 2026-03-13: Targeted dry-run validation succeeded for the new `BLL_External_Contact_Credential_RCD_Logical_Process` flow (`0AfRL00000dJdYf0AK`).
- 2026-03-13: Deployed `BLL_External_Contact_Credential_RCD_Logical_Process` to `OnboardV2` (`0AfRL00000dJdbt0AC`); the CLI surfaced a non-blocking reporting bug, so deploy success was confirmed via `sf project deploy report`.
- 2026-03-13: Targeted dry-run validation succeeded for the legacy-flow deactivation metadata (`0AfRL00000dJYxC0AW`).
- 2026-03-13: Deployed `BLL_External_Contact_Credential_RCD_Execute_Supplemental_Onboarding_Requirements.flowDefinition` with `activeVersionNumber=0` to inactivate the retired legacy flow in `OnboardV2` (`0AfRL00000dJZrf0AG`).
- 2026-03-13: Salesforce still reports a non-blocking info warning on `DOMAIN_OmniSObject_SFL_Send_Email_Communication` about the hard-coded email template id, so that legacy configuration remains a follow-up cleanup item.
- 2026-03-13: Hardened `RecordCollectionEditorConfigService` and `RecordCollectionEditorGetRecordsService` so metadata-driven field configs and relationship queries now filter inaccessible fields, enforce `WITH SECURITY_ENFORCED` on dynamic relationship lookups, and use user-mode DML for record-editor writes.
- 2026-03-13: Updated `RecordCollectionEditorAsyncService` to use `LoggingUtil` instead of ad hoc async debug logging, and tightened `ExperienceOpportunityCreateAsyncService` vendor-program fallback queries with `WITH SECURITY_ENFORCED`.
- 2026-03-13: Added `RecordCollectionEditorConfigServiceTest` coverage for config parsing, record/relationship creation, and relationship JSON serialization.
- 2026-03-13: Targeted dry-run validation succeeded for the record-editor async security batch (`0AfRL00000dKFu60AG`) with specified tests passing `10/10`.
- 2026-03-13: Deployed the record-editor async security batch to `OnboardV2` (`0AfRL00000dKGBp0AO`) with specified tests passing `10/10`.
- 2026-03-13: Hardened `Onboarding_Program_Sales_Team` and `Onboarding_Account_Services` permissions for the Record Collection Editor path by granting the active editor Apex classes plus `Contact` create/edit/read and the currently supported `Contact` / `AccountContactRelation` field permissions required by the tightened user-mode/FLS enforcement.
- 2026-03-13: Normalized the repo copies of those permission sets against `OnboardV2` by removing stale legacy object, field, tab, and Apex-class grants that no longer exist in the target org (for example `Background_Check__c`, dead `Onboarding__c` lookup fields, and retired `VendorOnboarding*` service classes).
- 2026-03-13: Targeted dry-run validation succeeded for the normalized permission-set bundle (`0AfRL00000dKKqX0AW`); the initial CLI response surfaced a temp-path packaging bug, so success was confirmed via `sf project deploy report`.
- 2026-03-13: Deployed the normalized permission-set bundle to `OnboardV2` (`0AfRL00000dKCEg0AO`); deploy success was again confirmed via `sf project deploy report` because the initial CLI output surfaced the same non-blocking temp-path bug.
- 2026-03-13: Replaced the remaining ad hoc `System.debug` usage in production classes `PicklistHelper` and `SalesforceMessagingProvider` with standardized `LoggingUtil` calls, preserving the existing fallback behavior while removing direct debug statements from the runtime path.
- 2026-03-13: Targeted dry-run validation succeeded for the logging cleanup batch (`0AfRL00000dKLcv0AG`) using a metadata-format deploy package to avoid the repo-local `UnsafeFilepathError` CLI bug on source-format deploys.
- 2026-03-13: Deployed the logging cleanup batch to `OnboardV2` (`0AfRL00000dKEZs0AO`).
- 2026-03-13: Added fault connectors to the reusable Record Collection Editor create subflows `DOMAIN_OmniSObject_SFL_Create_Contact` and `DOMAIN_OmniSObject_SFL_CREATE_Opportunity_Contact_Role` so unexpected Apex faults are now captured into the flows' existing output error variables instead of hard-failing the interview.
- 2026-03-13: Targeted dry-run validation succeeded for the Record Collection Editor flow-fault batch (`0AfRL00000dKM170AG`) using a metadata-format deploy package to avoid the repo-local `UnsafeFilepathError` CLI bug on source-format deploys.
- 2026-03-13: Deployed the Record Collection Editor flow-fault batch to `OnboardV2` (`0AfRL00000dKMCP0A4`).
- 2026-03-13: Added fault connectors to the experience-layer Record Collection Editor screen flows `EXP_Contact_SCR_Create_Contact` and `EXP_Opportunity_Contacts_SCR_CREATE_Opportunity_Contacts` so get-records and async-queue action faults now populate `errorMessage` instead of failing the screen flow outright; the opportunity-contact formatter now also maps the underlying `errorMessage` output from `RecordCollectionEditorGetRecordsService`.
- 2026-03-13: Targeted dry-run validation succeeded for the Record Collection Editor screen-flow fault batch (`0AfRL00000dKQzB0AW`) using a metadata-format deploy package to avoid the repo-local `UnsafeFilepathError` CLI bug on source-format deploys.
- 2026-03-13: Deployed the Record Collection Editor screen-flow fault batch to `OnboardV2` (`0AfRL00000dKQxa0AG`); the initial CLI response surfaced a non-blocking finalization/reporting bug, so deploy success was confirmed via `sf project deploy report`.
- 2026-03-13: Salesforce surfaced a non-blocking info warning that `EXP_Opportunity_Contacts_SCR_CREATE_Opportunity_Contacts` references `EXP_Contact_SCR_Create_Contact` as a subflow without an active version in the org; this did not block deployment but remains metadata hygiene follow-up debt.
- 2026-03-13: Hardened active domain flows `DOMAIN_OmniSObject_SFL_CREATE_Required_External_Contact_Credenti` and `DOMAIN_OmniSObject_SFL_CREATE_Training_Assignment_Records` so record-create failures now capture `$Flow.FaultMessage` into output `errorMessage` variables rather than failing silently or terminating without context; the required-external-credential flow now also surfaces a clear missing-input message on its validation path.
- 2026-03-13: Targeted dry-run validation succeeded for the domain-flow resiliency batch (`0AfRL00000dKe1H0AS`) using a metadata-format deploy package to avoid the repo-local `UnsafeFilepathError` CLI bug on source-format deploys.
- 2026-03-13: Deployed the domain-flow resiliency batch to `OnboardV2` (`0AfRL00000dKh7C0AS`); the initial CLI response surfaced a non-blocking finalization/reporting bug, so deploy success was confirmed via `sf project deploy report`.
- 2026-03-13: Salesforce surfaced non-blocking info warnings that both active domain flows still run in `SystemModeWithoutSharing`, and that `DOMAIN_OmniSObject_SFL_CREATE_Training_Assignment_Records` calls `Recalculate_Onboarding_Training_Status` from a parent flow running in system mode while the child remains in default mode; these context warnings remain follow-up hardening debt.
- 2026-03-13: Hardened active domain flow `DOMAIN_OmniSObject_SFL_CREATE_Onboarding_Record` so onboarding create, single-record update, collection update, and training-status update DML paths now capture `$Flow.FaultMessage` into an output `errorMessage` variable instead of failing silently.
- 2026-03-13: Targeted dry-run validation succeeded for the `DOMAIN_OmniSObject_SFL_CREATE_Onboarding_Record` hardening pass (`0AfRL00000dKjKH0A0`) using a metadata-format deploy package to avoid the repo-local CLI source-format packaging/finalization bug.
- 2026-03-13: Deployed the hardened `DOMAIN_OmniSObject_SFL_CREATE_Onboarding_Record` flow to `OnboardV2` (`0AfRL00000dKYjq0AG`); the initial CLI response surfaced the known non-blocking finalization/reporting bug, so deploy success was confirmed via `sf project deploy report`.
- 2026-03-13: Wired active callers of `DOMAIN_OmniSObject_SFL_CREATE_Onboarding_Record` to convert nonblank onboarding-flow `errorMessage` outputs into shared fault-log records via `DOMAIN_OmniSObject_SFL_CREATE_Fault_Message`, creating terminal `Follow_Up_Queue__c` records with `Follow_Up_Type__c = In-App` and `Status__c = Failed` instead of silently swallowing parent-flow failures.
- 2026-03-13: Targeted dry-run validation succeeded for the onboarding fault-notification bundle (`0AfRL00000dKeMF0A0`) using a metadata-format deploy package to avoid the repo-local CLI source-format packaging/finalization bug.
- 2026-03-13: Deployed the onboarding fault-notification bundle to `OnboardV2` (`0AfRL00000dKkOQ0A0`); the initial CLI response surfaced the known non-blocking finalization/reporting bug, so deploy success was confirmed via `sf project deploy report`.
- 2026-03-13: Salesforce surfaced non-blocking info warnings that `DOMAIN_OmniSObject_SFL_CREATE_Training_Assignment_Records` still runs in `SystemModeWithoutSharing` and now calls both `Recalculate_Onboarding_Training_Status` and `Handle_Recalculate_Onboarding_Training_Status_Fault` subflows that remain in default flow mode; this context mismatch remains follow-up hardening debt.
- 2026-03-13: Enriched `DOMAIN_OmniSObject_SFL_CREATE_Fault_Message` so queued admin fault records now include source flow API, source element API, context key, onboarding id when available, and running user id inside `Follow_Up_Queue__c.Error_Message__c`.
- 2026-03-13: Wired active `BLL_LearnUponContactEnrollment_RCD_Connect_Logical_Records` to map the hardened `DOMAIN_OmniSObject_SFL_CREATE_Training_Assignment_Records.errorMessage` output into shared `Follow_Up_Queue__c` fault logging instead of silently swallowing LearnUpon training-assignment creation failures.
- 2026-03-13: Targeted dry-run validation succeeded for the shared-fault-context / LearnUpon fault-notification bundle (`0AfRL00000dKlSX0A0`) using a metadata-format deploy package.
- 2026-03-13: Deployed the shared-fault-context / LearnUpon fault-notification bundle to `OnboardV2` (`0AfRL00000dKlXN0A0`).
- 2026-03-13: Salesforce surfaced non-blocking info warnings that active `BLL_LearnUponContactEnrollment_RCD_Connect_Logical_Records` still carries legacy subflow-contract drift against current `DOMAIN_OmniSObject_SFL_CREATE_Training_Assignment_Records`, `DOMAIN_OmniSObject_SFL_GET_Training_Requirement_Records`, and `DOMAIN_OmniSObject_SFL_GET_Vendor_Program` versions, plus a default-mode/system-mode mismatch on training-assignment creation; deployment was not blocked.
- 2026-03-16: Added first-pass onboarding-requirement subject fulfillment runtime metadata in source and `OnboardV2`: new object `Onboarding_Requirement_Subject__c`, new `Fulfillment_Policy_Key__c` fields on `Vendor_Program_Requirement__c` and `Onboarding_Requirement__c`, and new CMDT `Onboarding_Fulfillment_Policy__mdt`.
- 2026-03-16: Updated `DOMAIN_OmniSObject_SFL_CREATE_Onboarding_Requirements` so onboarding requirements now snapshot `Fulfillment_Policy_Key__c` and delegate subject expansion to new subflow `DOMAIN_OmniSObject_SFL_CREATE_Onboarding_Requirement_Subjects`.
- 2026-03-16: Deployed the initial onboarding-requirement subject-fulfillment object/field/CMDT/flow bundle to `OnboardV2` (`0AfRL00000dMapd0AC`).
- 2026-03-16: Revised subject expansion to source contact-scoped subjects from `AccountContactRelation` rather than `Contact`, including `Principal Owner` resolution and `Role_Snapshot__c` capture, and deployed the update to `OnboardV2` (`0AfRL00000dMxXg0AK`).
- 2026-03-16: Observed current first-pass boundary: subject rows are created, but evaluator/aggregator logic is still missing for evidence matching, subject status recalculation, and parent `Onboarding_Requirement__c` rollup.
- 2026-03-16: Hardened `DOMAIN_OmniSObject_SFL_CREATE_Onboarding_Requirement_Subjects` so missing fulfillment policies, unsupported subject models, and subject-record create faults now route into shared `Follow_Up_Queue__c` admin fault logging through `DOMAIN_OmniSObject_SFL_CREATE_Fault_Message`.
- 2026-03-16: Updated `DOMAIN_OmniSObject_SFL_CREATE_Onboarding_Requirements` so `Creation_of_Record_Completed__c` is no longer marked true when subject expansion returns a nonblank `errorMessage`.
- 2026-03-16: Targeted dry-run validation succeeded for the onboarding-requirement subject hardening bundle (`0AfRL00000dNF4j0AG`) using a metadata-format deploy package.
- 2026-03-16: Deployed the onboarding-requirement subject hardening bundle to `OnboardV2` (`0AfRL00000dN50Z0AS`); the initial CLI response surfaced the known non-blocking finalization/reporting bug, so deploy success was confirmed via `sf project deploy report`.
- 2026-03-16: Added reusable getter flows `DOMAIN_OmniSObject_SFL_GET_Onboarding_Requirement` and `DOMAIN_OmniSObject_SFL_GET_Onboarding_Fullfilment_Policy`, then refactored `DOMAIN_OmniSObject_SFL_CREATE_Onboarding_Requirement_Subjects` to bulk-fetch onboarding requirements and fulfillment policies outside the requirement loop instead of doing `Get Records` inside the loop.
- 2026-03-16: Refactored `DOMAIN_OmniSObject_SFL_CREATE_Onboarding_Requirement_Subjects` to reuse a single `DOMAIN_OmniSObject_SFL_GET_Account_Contact_Relationship` call plus in-memory role filtering for `Principal Owner`, and shortened that getter’s collection output contract to `AccountContactRelCollection` so parent-flow subflow mappings stay within Salesforce’s 40-character output-name limit.
- 2026-03-16: Deployed the new reusable onboarding-requirement / fulfillment-policy getter flows to `OnboardV2` (`0AfRL00000dN9SM0A0`); the initial CLI response surfaced the known non-blocking finalization/reporting bug, so deploy success was confirmed via `sf project deploy report`.
- 2026-03-16: Targeted dry-run validation succeeded for the subject-flow bulkification / ACR-reuse refactor (`0AfRL00000dNFZN0A4`) using a metadata-format deploy package.
- 2026-03-16: Deployed the subject-flow bulkification / ACR-reuse refactor bundle to `OnboardV2` (`0AfRL00000dNFfp0AG`); the initial CLI response surfaced the known non-blocking finalization/reporting bug, so deploy success was confirmed via `sf project deploy report`.
- 2026-03-16: Added reusable getter flow `DOMAIN_OmniSObject_SFL_GET_Onboarding_Requirement_Subjects`, retained `DOMAIN_OmniSObject_SFL_EVALUATE_Onboarding_Requirement_Subject` as the canonical parent-aggregation flow, and added async record-trigger flow `BLL_Onboarding_Requirement_Subject_RCD_Logical_Process` so subject status changes now recalculate parent `Onboarding_Requirement__c.Status__c` and `Completed__c` using filtered-collection counters instead of ad hoc loop logic.
- 2026-03-16: Targeted dry-run validation succeeded for the onboarding-requirement subject rollup bundle (`0AfRL00000dNBvb0AG`) using a metadata-format deploy package.
- 2026-03-16: Deployed the onboarding-requirement subject aggregation bundle to `OnboardV2` (`0AfRL00000dNGLl0AO`); Salesforce returned one non-blocking info warning during deploy that the new subject trigger referenced the canonical evaluation flow in the same deployment transaction, but all three flows activated successfully.
- 2026-03-16: Standardized subject-fulfillment getter naming to the `SFL_GET_*` convention by deploying `DOMAIN_OmniSObject_SFL_GET_Onboarding_Fullfilment_Policy`, `DOMAIN_OmniSObject_SFL_GET_Onboarding_Requirement_Subjects`, and the org-aligned `DOMAIN_OmniSObject_SFL_GET_Onboarding_Requirement`, then updated `DOMAIN_OmniSObject_SFL_CREATE_Onboarding_Requirement_Subjects` and the canonical evaluator flow to use those APIs (`0AfRL00000dNHRV0A4` after dry-run `0AfRL00000dNHGD0A4`).
- 2026-03-16: Deleted legacy getter definitions `DOMAIN_OmniSObject_GET_Onboarding_Fullfilment_Policy` and `DOMAIN_OmniSObject_GET_Onboarding_Requirement_Subjects` from `OnboardV2` after the obsolete dependent flows and historical versions were removed.
- 2026-03-16: Replaced repo-local `DOMAIN_OmniSObject_SFL_ROLLUP_Onboarding_Requirement_From_Subjects` with the retained org flow `DOMAIN_OmniSObject_SFL_EVALUATE_Onboarding_Requirement_Subject`, updated `BLL_Onboarding_Requirement_Subject_RCD_Logical_Process` to call the evaluator, validated the cutover (`0AfRL00000dNI930AG`), deployed it to `OnboardV2` (`0AfRL00000dNIAf0AO`), and retired the old rollup flow definition from the org.
- 2026-03-17: Extended `DOMAIN_OmniSObject_SFL_EVALUATE_Onboarding_Requirement_Subjects_By_Evidence` to support `Training_Assignment__c` statuses, expose a reusable `MatchedSubjectCount` output via the flow counter pattern, expanded canonical getter `DOMAIN_OmniSObject_SFL_GET_Onboarding_Requirement_Subjects` to support subject-based retrieval, deleted the now-redundant repo-only `DOMAIN_OmniSObject_SFL_GET_Onboarding_Requirement_Subjects_By_Subject`, validated the active training-assignment evidence bundle (`0AfRL00000dO3kg0AC`), and deployed it to `OnboardV2` (`0AfRL00000dO5bB0AS`) so `BLL_OmniSObject_RCD_SYNC_Training_Assignments` now drives contact-first/account-fallback subject evaluation.
- 2026-03-17: Added the explicit orchestration path for training evidence so `DOMAIN_OmniSObject_SFL_EVALUATE_Onboarding_Requirement_Subjects_By_Evidence` now returns `ChangedOnboardingRequirementIdCollection`, and `BLL_OmniSObject_RCD_SYNC_Training_Assignments` explicitly loops those changed parent requirement ids and invokes `DOMAIN_OmniSObject_SFL_EVALUATE_Onboarding_Requirement_Subject` after onboarding sync work completes; targeted dry-run validation succeeded (`0AfRL00000dO8KX0A0`) and deployment to `OnboardV2` succeeded (`0AfRL00000dO8M90AK`). Salesforce returned only non-blocking info warnings that the same deployment bundle validated the new subflow output mapping against active version 1 of the evidence flow.
- 2026-03-17: Resolved repo-ahead-of-org deploy: disabled source tracking (`sf org disable tracking`) to fix `UnsafeFilepathError` from cached temp path in `.sf/orgs/.../localSourceTracking`; renamed new flow from `DOMAIN_OmniSObject_SFL_EVALUATE_Onboarding_Requirement_Subjects_For_Onboarding_Record` (85 chars) to `DOMAIN_OmniSObject_SFL_EVAL_OnbReq_Subjects_For_OnbRec` (55 chars) to satisfy Flow API name 80-character limit; deployed flow bundle to `OnboardV2` (`0AfRL00000dOE1l0AG`) via targeted `--metadata` deploy (BLL_OmniSObject_RCD_SYNC_Training_Assignments, DOMAIN_OmniSObject_SFL_EVALUATE_Onboarding_Requirement_Subjects_By_Evidence, DOMAIN_OmniSObject_SFL_GET_Onboarding_Requirement_Subjects, DOMAIN_OmniSObject_SFL_EVAL_OnbReq_Subjects_For_OnbRec); deploy success confirmed via `sf project deploy report` after CLI finalization bug; full-scope metadata deploy still blocked by pre-existing org/repo drift (permission sets, objects, expression sets).
- 2026-03-17: Standardized onboarding-requirement subject evaluator flow naming to `DOMAIN_OmniSObject_SFL_EVAL_Onb_Req_Subjects_*`: `_Parent` (child→parent rollup), `_By_Evidence` (evidence-to-subject status mapping), `_For_Onb_Rec` (onboarding-record context orchestrator); retired legacy flows via destructiveChanges; updated callers `BLL_Onboarding_Requirement_Subject_RCD_Logical_Process`, `BLL_OmniSObject_RCD_SYNC_Training_Assignments`; consolidation rationale documented in subject-fulfillment doc.
- 2026-03-17: Legacy and retired evaluator flows removed from `OnboardV2` org; flow naming cutover complete.
- 2026-03-17: Added `OnboardingRequirementSubjectInvocable` for idempotent subject creation (pre-filters by existing `Unique_Key__c`); `DOMAIN_OmniSObject_SFL_CREATE_Onboarding_Requirement_Subjects` now uses this invocable instead of recordCreates; added `OnbReqSubjectInvocableTest`; dry-run validation succeeded.
- 2026-03-17: Added `Makefile` with `make audit` and targets for all `scripts/automation/*` scripts; added `audit-flow-fault-connectors.sh` for flow fault-connector gap audit.
- 2026-03-17: Removed legacy flows `EXP_Contact_SCR_Set_Up_Agent_Vendor_Programs` and `DOMAIN_Contact_SFL_GET_Account_Information_from_Contact_Record` from repo; removed `Contact.Set_Up_Agent_on_Vendor` quick action; added all three to `destructiveChanges.xml` for org cleanup; updated manifests and docs.
- 2026-03-17: Added fault connectors to high-priority flows: `DOMAIN_OmniSObject_SFL_CREATE_Onboarding_Requirements` (Fail_Fast, Create_Onboarding_Requirements, both Update_Onboarding_Record paths), `BLL_BRE_Evaluate_Business_Rules` (all 4 actionCalls), `DOMAIN_OmniSObject_SFL_CREATE_Follow_Up_Queue` (Create/Update), `EXP_Contract_SCR_Send_Adobe_Agreement` (Update_Contract_Record), `DOM_Onboarding_SFL_CREATE_Training_Assignment_Records` (Create_Training_Assignment_Onboarding_Link, Create_Training_Assignment_Records); fault paths route to shared `DOMAIN_OmniSObject_SFL_CREATE_Fault_Message` or capture into output variables; dry-run validation succeeded.
- 2026-03-18: Completed targeted review requested for `BLL_Onboarding_Requirement_RCD_Logical_Process`, `DOMAIN_OmniSObject_SFL_CREATE_Related_Onboarding_Requirement_Records`, and `EXP_Opportunity_SCR_Create_Record` plus related action/CMDT paths; identified current fault-path, output-contract, and CMDT hygiene gaps; added sequenced remediation plan in "Targeted Gap Review (2026-03-18)" below.
- 2026-03-18: Re-ran `audit-flow-fault-connectors`; current report shows 51 unresolved `recordCreates`/`recordUpdates`/`actionCalls` fault-connector gaps, including 9 in the focal `BLL_BRE_Evaluate_Business_Rules` + `BLL_Onboarding_Requirement_RCD_Logical_Process` + `EXP_Opportunity_SCR_Create_Record` chain; script scope currently excludes `subflows` and `recordLookups`.
- 2026-03-18: Completed focal-chain `P0` remediation for `BLL_BRE_Evaluate_Business_Rules`, `BLL_Onboarding_Requirement_RCD_Logical_Process`, `DOMAIN_OmniSObject_SFL_CREATE_Related_Onboarding_Requirement_Records`, and `EXP_Opportunity_SCR_Create_Record` by adding missing fault connectors, explicit output-error branching, and shared `DOMAIN_OmniSObject_SFL_CREATE_Fault_Message` escalation.
- 2026-03-18: Re-ran `audit-flow-fault-connectors` after remediation; unresolved audit-scope gaps dropped from 51 to 42, and the focal chain now reports zero no-fault entries (`yes` on all targeted elements).
- 2026-03-18: Local XML integrity checks (`xmllint --noout`) passed for all four updated focal flows; org deploy/test validation is still pending in this pass.
- 2026-03-18: Aligned agreement evidence-to-status routing for the new subject model by updating `DOMAIN_OmniSObject_SFL_EVAL_Onb_Req_Subjects_By_Evidence` (`Signed` now maps to `Setup Complete`) and adding `Onboarding_Status_Normalization.agreement_paperwork_sent` (`Agreement + Paperwork Sent -> Out for signature`) so `OnboardingStatusEvaluatorInvocable` can resolve the correct CMDT-driven rule path.
- 2026-03-18: Added `OnboardingStatusEvaluatorTest.testEvaluatePaperworkSentMapsToOutForSignature` and validated the flow/CMDT/test bundle with targeted dry-run deploy `0AfRL00000dPrdl0AC`; deployment to target org was then completed.
- 2026-03-18: Captured CMDT alias-drift baseline for next `P1` remediation wave: duplicate alias sets currently exist across `Onboarding_Default_Vendor_Program` (1), `Onboarding_Fulfillment_Policy` (4), and `Onboarding_Status_Normalization` (152 shared record names across `Type.*` and `Type__mdt.*` source files).
- 2026-03-18: Reconciled checklist completion against repo state: confirmed all `@AuraEnabled` / `@InvocableMethod` classes are `with sharing`, validated active trigger recursion/diff/bulk (>200) hardening in `TerritoryAssignmentsTrigger` + `EmailCommTerritoryRoleHelperTest`, and confirmed automation scripts use `sf` CLI without legacy `sfdx` command usage.
- 2026-03-18: Canonicalized onboarding CMDT record aliases to `Type__mdt.RecordName` naming (`Onboarding_Default_Vendor_Program*`, `Onboarding_Fulfillment_Policy*`, `Onboarding_Status_Normalization*`): removed 157 duplicate `Type.*` aliases, migrated 4 base-only normalization records (`agreement_out_for_signature`, `agreement_paperwork_sent`, `agreement_signed`, `contract_complete`), and added automation scripts `check-cmdt-alias-drift.sh` + `canonicalize-onboarding-cmdt-aliases.sh` with `Makefile` targets.
- 2026-03-18: Aligned evaluator code to active CMDT rule set by removing unused `OnboardingStatusEvaluatorService` condition branches (`HAS_REQUIREMENTS_NOT_ALL_SETUP_COMPLETE`, `OPPORTUNITY_UNCANCELED`) and adding `@TestVisible` branch-evaluation coverage hook for deterministic test assertions.
- 2026-03-18: Expanded `OnboardingStatusEvaluatorTest` and `OnboardingDefaultVendorProgramInvocTest` for rule-order and flow-contract assertions (missing-scenario, ambiguous match, and `Vendor_Program_Name_Contains__c` path), added CMDT record `Onboarding_Default_Vendor_Program__mdt.REQUIRE_NDA_NAME_CONTAINS`, and validated with targeted dry-run deploy `0AfRL00000dPzHx0AK` (`14/14` tests passing).
- 2026-03-18: Added `validate-focal-flow-error-paths.sh` and `make validate-focal-flow-error-paths` to enforce focal fault-path/error-propagation assertions; current run passes `18/18` checks.
- 2026-03-18: Added runtime Flow interview coverage `RelatedOnboardingReqFlowTest` for `DOMAIN_OmniSObject_SFL_CREATE_Related_Onboarding_Requirement_Records`, asserting deterministic default-path errors, unsupported onboarding-only branch behavior, agreement child-flow error propagation, and no-error non-related-type behavior; targeted dry-run validation passed (`0AfRL00000dQ6g10AC`, `4/4` tests).
- 2026-03-18: Updated `OnboardingDefaultVendorProgramInvocTest` to align with canonical scenario key `REQUIRE_NDA_NAME_CONTAINS` so ambiguous/default-vendor assertions remain deterministic after CMDT alias canonicalization.
- 2026-03-18: Generated focused deploy manifests for this wave (`manifest/best-practices-review-2026-03-18/package.xml`, `destructiveChangesPost.xml`) directly from changed metadata and validated with dry-run deploy `0AfRL00000dPyqZ0AS` (`176` components, `18/18` tests passing).
- 2026-03-18: Deployed the focal-wave manifest + post-destructive CMDT cleanup to `OnboardV2` (`0AfRL00000dQ7U10AK`); deployment succeeded with `176` components and `18/18` tests passing.
- 2026-03-18: Fixed `objectRelatedList` inline-edit picklist regression where only the current value appeared for `Onboarding_Requirement__c.Status__c` in related lists: updated `objectRelatedList.js` to normalize missing record types to master/default ids and fall back to object metadata picklist values when UI API picklist fetches fail or return empty; deployed the LWC bundle to `OnboardV2` (`0AfRL00000dPuoa0AC`) and confirmed deploy report `Succeeded` (`1/1` components, `0` errors).
- 2026-03-18: Applied follow-up `objectRelatedList` picklist-cache fix after continued one-value inline dropdown reports: non-empty option checks now gate cache reuse, empty cached arrays no longer short-circuit fallback selection, and per-record-type options are reloaded/fallbacked when prior cache entries were empty; deployed to `OnboardV2` (`0AfRL00000dQB9T0AW`, `Succeeded`, `1/1` components, `0` errors).
- 2026-03-18: Rolled `objectRelatedList` back to the committed pre-hotfix baseline after continued inline picklist regression reports (`Status__c` still showing only current value) to re-establish a known-good starting point before reintroducing hardening in smaller increments; deployed rollback bundle to `OnboardV2` (`0AfRL00000dQBUQ0A4`, `Succeeded`, `1/1` components, `0` errors).
- 2026-03-18: Validated org/runtime metadata path for the failing example (`ONB-10211`): `Onboarding_Requirement__c` rows carry distinct record types (`Contract`, `Agreement Requirements`), and direct UI API calls return multi-value `Status__c` picklists for both record types, confirming server-side picklist metadata is healthy while the LWC runtime path remains suspect.
- 2026-03-18: Deployed targeted `objectRelatedList` race/empty-cache hardening (no `MASTER_RECORD_TYPE_ID`): added one-time post-`getObjectInfo` refresh when rows are missing `RecordTypeId`, prevented empty option arrays from being treated as loaded cache entries, and allowed retry on empty cached states (`0AfRL00000dQAog0AG`, `Succeeded`, `1/1` components, `0` errors).
- 2026-03-18: Added picklist-fetch telemetry to `objectRelatedList` (request/success/empty-fallback/error-fallback counters, last field/recordType/outcome details, and console event logs) so runtime picklist loading behavior can be observed directly from the related-list header during repro.
- 2026-03-18: Re-deployed telemetry-enabled `objectRelatedList` bundle to `OnboardV2` (`0AfRL00000dQEP30AO`, `Succeeded`, `1/1` components, `0` errors) after user-reported repro persisted across browser/session changes.
- 2026-03-18: Telemetry repro confirmed client-side UI API picklist calls were failing with `Session expired or invalid` (`req:4 ok:0 errorFallback:4`) for `Onboarding_Requirement__c.Status__c`, explaining the single-value inline dropdown regression.
- 2026-03-18: Added resilient Apex describe fallback path for `objectRelatedList` picklists via new `ObjectRelatedListController.getPicklistFieldFallbackOptions`; the LWC now caches field-level Apex fallback options, auto-disables repeated UI API fetch attempts after auth-session failures, and continues to expose telemetry for request/outcome counts.
- 2026-03-18: Deployed `ObjectRelatedListController` + `objectRelatedList` fallback patch to `OnboardV2` (`0AfRL00000dQ7yh0AC`, `Succeeded`, `2/2` components, `0` errors).
- 2026-03-18: User validation showed prior Apex fallback returned full-field `Status__c` values (not record-type-scoped), creating option supersets in related-list inline edit compared to direct record edit.
- 2026-03-18: Reworked fallback to use record-type-scoped Apex UI API retrieval: added `ObjectRelatedListController.getPicklistFieldOptionsByRecordType` (server-side callout to UI API picklist endpoint with session bearer) and updated `objectRelatedList` to request fallback options per `fieldApiName + recordTypeId`; describe fallback now only applies as a last-resort safety path.
- 2026-03-18: Deployed record-type-scoped fallback patch to `OnboardV2` (`0AfRL00000dQDrC0AW`, `Succeeded`, `2/2` components, `0` errors).
- 2026-03-18: User validation still reported incorrect supersets and slower rendering; telemetry remained dominated by client-side session failures, indicating per-field fetch/callout fallback path was still too expensive and unstable.
- 2026-03-18: Refactored `objectRelatedList` picklist loading to use `getPicklistValuesByRecordType` wire adapter (one request per record type, all fields) instead of per-field `fetch`/Apex-callout sequence; added in-component queueing to load record types sequentially and cache scoped options.
- 2026-03-18: Updated picklist row-decoration behavior to avoid broad field-level fallback while scoped options are still loading (interim editor shows current value only), preventing incorrect supersets from appearing before scoped cache warmup.
- 2026-03-18: Deployed wire-adapter picklist loader refactor to `OnboardV2` (`0AfRL00000dQFOL0A4`, `Succeeded`, `1/1` components, `0` errors).
- 2026-03-18: User validated `objectRelatedList` inline picklist behavior is now correct and performant on `ONB-10211`; related-list `Status__c` options match direct record-edit expectations and telemetry confirms healthy wire path (`wireError:0`, successful scoped loads).
- 2026-03-18: Cleanup pass after validation: removed now-unused Apex picklist fallback endpoints from `ObjectRelatedListController` (`getPicklistFieldFallbackOptions`, `getPicklistFieldOptionsByRecordType`, and helper callout method) so active runtime path is only `getRelatedRecords` + UI API wire adapters.
- 2026-03-18: UX cleanup: `objectRelatedList.enablePicklistTelemetry` default was changed to `false` in component metadata to keep related-list headers clean unless telemetry is explicitly enabled for troubleshooting.
- 2026-03-18: First incremental hardening pass: `objectRelatedList` picklist record-type wire handler now uses guarded completion (`completePicklistRecordTypeLoad`) and `try/catch/finally` so request-key/queue state is always released even on unexpected runtime exceptions; deployed cleanup + hardening bundle to `OnboardV2` (`0AfRL00000dQFTB0A4`, `Succeeded`, `2/2` components, `0` errors).
- 2026-03-18: Removed HTML troubleshooting output from `objectRelatedList` header (telemetry line) and deleted the now-unused `.telemetry-text` style while keeping backend telemetry logic available in code for future diagnostics; deployed UI cleanup to `OnboardV2` (`0AfRL00000dQCbn0AG`, `Succeeded`, `1/1` components, `0` errors).
- 2026-03-19: Synced repo-tracked flow metadata to `OnboardV2` as source-of-truth with an intersection-only retrieve (repo members only, no net-new flows): `78` `Flow` members + `4` `FlowDefinition` members from manifest `/tmp/repo_flow_sync_package.xml`; retrieve succeeded and updated existing source files only.
- 2026-03-19: Flow fault-connector hardening wave 1 completed for `BLL_Order_RCD_Business_Logic`, `DOMAIN_Agreement_SFL_CREATE_Agreement`, `DOMAIN_OmniSObject_SFL_CREATE_Agreement`, `DOMAIN_OmniSObject_SFL_CREATE_Contract`, `DOMAIN_OmniSObject_SFL_CREATE_Onboarding_Requirements`, and `DOMAIN_OmniSObject_SFL_CREATE_Related_Onboarding_Requirement_Records`; targeted dry-run deploy succeeded (`0AfRL00000dR9lV0AS`).
- 2026-03-19: Additional hardening landed for `BLL_LearnUponContactEnrollment_SCD_Review_Assign_Missing_Onboarding_Records` and `BLL_Onboarding_SCD_Review_and_Assign_Missing_Training_Records`; combined targeted dry-run deploy succeeded (`0AfRL00000dR9yP0AS`).
- 2026-03-19: Attempted to add fault connectors to five before-save Fast Field Updates flows (`BLL_External_Contact_Credential_RCD_Prevent_Duplicates`, `BLL_External_Contact_Credential_Type_RCD_Prevent_Duplicates`, `BLL_Training_Assignment_Onboarding_RCD_Prevent_Duplicates`, `BLL_Training_Assignment_RCD_Populate_Unique_Assignment_Key`, `BLL_Vendor_Program_Training_Requirement_RCD_Prevent_Duplicates`) failed validation (`0AfRL00000dQxlz0AC`) due Salesforce platform constraint: before-save `Update Records` elements cannot have fault connectors; invalid edits were reverted.
- 2026-03-19: `make audit-fault-connectors` gap count reduced from `43` to `26` after valid wave-1/wave-2 hardening, with the five before-save flows now tracked as platform-exempt from fault-connector remediation.
- 2026-03-19: Scoped continuation per user direction: ignored non-OmniSObject `DOM_*` / non-`DOMAIN_OmniSObject_*` flow families for this pass (except `EXP_*`/`BLL_*_Logical_Process` as explicitly allowed).
- 2026-03-19: Completed remaining non-exempt fault-connector hardening for `DOMAIN_OmniSObject_SFL_CREATE_Dealer_Training`, `DOMAIN_OmniSObject_SFL_CREATE_Follow_Up_Queue`, `DOMAIN_OmniSObject_SFL_CREATE_LearnUponContactEnrollment`, `DOMAIN_OmniSObject_SFL_CREATE_LearnUpon_Contact_Enrollment_Membership_Record`, `DOMAIN_OmniSObject_SFL_CREATE_LearnUpon_Group_Membership`, `DOMAIN_OmniSObject_SFL_CREATE_Opportunity`, `DOMAIN_OmniSObject_SFL_CREATE_Contact_Fields_with_ECC_Information`, `DOMAIN_OmniSObject_SFL_CREATE_Contact_Training_Assignment_Record`, `DOMAIN_OmniSObject_SFL_CREATE_External_Contact_Credentials`, `DOMAIN_OmniSObject_SFL_CREATE_Process_Status_Per_Business`, `DOMAIN_OmniSObject_SFL_Send_Email_Communication`, and `EXP_Contract_SCR_Send_Adobe_Agreement`.
- 2026-03-19: Targeted dry-run validation for the 12-flow hardening bundle succeeded (`0AfRL00000dRAo10AG`).
- 2026-03-19: Re-ran `make audit-fault-connectors`; unresolved no-fault entries reduced from `26` to `5`, with only the known before-save Fast Field Updates platform-exempt flows remaining.
- 2026-03-19: Investigated deployment blocker on `BLL_Training_Assignment_SCD_Training_Reminder_Emails` where `Send_Email_and_Log_Communication` pointed to stale action id `01pRL00000WBaqXNotFound`; root cause was residual legacy EmailComm batch-send wiring in flow metadata.
- 2026-03-19: Reworked `BLL_Training_Assignment_SCD_Training_Reminder_Emails` to use the current communication send path (`DOMAIN_OmniSObject_ACTION_Send_Communication_Template`) via subflow call per matched reminder context, removed legacy `EmailCommSendRequestDTO` payload variables/assignments, and replaced post-send handling with explicit `Did_Send_Email_Fail` decision routing into shared fault logging.
- 2026-03-19: Targeted dry-run validation for updated reminder flow succeeded (`0AfRL00000dRCY80AO`), unblocking this flow from the prior missing-action deployment failure.
- 2026-03-19: Replaced the interim per-record subflow send in `BLL_Training_Assignment_SCD_Training_Reminder_Emails` with bulk invocable `CommunicationTemplateBulkSendAction`; flow now accumulates parallel ID lists (`communicationTemplateIds`, `recipientIds`, `onboardingIds`) and performs a single bulk send call per training-assignment iteration.
- 2026-03-19: Aligned bulk send behavior with standard Send Email semantics in Apex (`setTargetObjectId` for Contact activity logging, `setWhatId` for related `Onboarding__c`, `setOrgWideEmailAddressId` via configurable sender address input) while retaining aggregate fault output (`sendFailed`, `errorMessage`) for shared flow fault handling.
- 2026-03-19: Initial validation failures were resolved by removing unsupported Apex-defined flow field assignment paths and switching to list-based flow inputs; targeted dry-run compile then succeeded (`0AfRL00000dRHMT0A4`).
- 2026-03-19: Added/expanded `CommunicationTemplateBulkSendActionTest` coverage for empty payload, invalid rows, size mismatch, onboarding-id validation, and template-lookup/send branches; targeted dry-run with `RunSpecifiedTests` succeeded (`0AfRL00000dRDkI0AW`, `4/4` passing).
- 2026-03-19: Deployed the bulk reminder-email refactor bundle to `OnboardV2` (`0AfRL00000dRDdq0AG`) with `RunSpecifiedTests` on `CommunicationTemplateBulkSendActionTest` (`4/4` passing).
- 2026-03-19: Refactored `DOMAIN_OmniSObject_SFL_CREATE_Onboarding_Requirements` to remove per-item `RecordType` gets/fail-fast inside the `Loop_VendorProgramRequirements` path: added one upfront `Get_Onboarding_Requirement_RecordTypes` fetch, in-memory developer-name match loop per requirement, and a single outside-loop fail-fast gate (`Should_Fail_Fast_Missing_RecordType_Mapping`) before create DML.
- 2026-03-19: Targeted dry-run validation for the refactored onboarding-requirements flow succeeded (`0AfRL00000dRIjx0AG`), then deployment to `OnboardV2` succeeded (`0AfRL00000dRIYg0AO`).
- 2026-03-19: Removed remaining ad hoc production-class `System.debug` statements (outside `LoggingUtil`) across EmailTemplateSync, FollowUp, Twilio SMS provider, and Record Collection Editor async classes by standardizing on `LoggingUtil.info/warn/error`.
- 2026-03-19: Production debug-logging hardening batch validated in dry-run deploy (`0AfRL00000dRJhe0AG`) and deployed to `OnboardV2` (`0AfRL00000dRJjF0AW`).
- 2026-03-19: Added loop-governance automation scripts (`audit-flow-loop-shape.sh`, `audit-flow-loop-connectors.sh`) to first-class `make` targets and included both in the aggregate `make audit` path.
- 2026-03-19: Re-ran `make audit-fault-connectors audit-flow-loop-shape audit-flow-loop-connectors`; fault-gap output remains at the 5 known before-save platform exceptions, and loop-shape audit reports `Lookup count: 0` and `DML/action count: 0` for direct loop-variable references.
- 2026-03-19: Enhanced loop-connector audit classification to recognize `collectionProcessors` so loop-target typing is explicit rather than `(unknown)` for filter-collection targets.
- 2026-03-19: Current loop-connector audit reports `0` direct `nextValueConnector` paths into `recordLookups`/`recordCreates`/`recordUpdates`/`recordDeletes`/`actionCalls`; only one `nextValue -> subflow` review candidate remains (`BLL_Onboarding_SFL_Dispatch_Communication_By_Context` -> `Get_Communication_Dispatch_Policy`).
- 2026-03-19: Refactored `BLL_Onboarding_SFL_Dispatch_Communication_By_Context` to remove `Loop_Communication_Templates.nextValueConnector -> Get_Communication_Dispatch_Policy`; dispatch-policy lookup is now preloaded once per recipient bucket (`Contact`, `PrincipalOwner`, `Agent`, and explicit input `RecipientType`) and selected in-loop via assignment/decision without a subflow hop on `nextValue`.
- 2026-03-19: Re-ran `make audit-fault-connectors audit-flow-loop-shape audit-flow-loop-connectors` after dispatch-loop refactor; fault-connector gaps remain at the same 5 before-save platform exceptions, loop-shape remains `Lookup count: 0` and `DML/action count: 0`, and loop-connector audit now reports `0` `nextValue -> subflow` review candidates.
- 2026-03-19: Converted `BLL_Onboarding_SFL_Dispatch_Communication_By_Context` recipient send path from per-recipient `DOMAIN_OmniSObject_ACTION_Send_Communication_Template` subflow calls to batched `CommunicationTemplateBulkSendAction` Apex action input lists (`communicationTemplateIds`, `recipientIds`, `onboardingIds`) with per-template send execution.
- 2026-03-19: Added explicit email-invocation protection in shared `CommunicationTemplateBulkSendAction`: calculates remaining `Messaging.sendEmail` invocation budget from `Limits.getLimitEmailInvocations() - Limits.getEmailInvocations()`, caps attempted sends to allowed chunk count, and flags skipped rows as failed with a bounded error message.
- 2026-03-19: Targeted dry-run validation for `BLL_Onboarding_SFL_Dispatch_Communication_By_Context` + `CommunicationTemplateBulkSendAction` succeeded (`0AfRL00000dRRiP0AW`).
- 2026-03-19: Executed `CommunicationTemplateBulkSendActionTest` in `OnboardV2`; run id `707RL00001Iey9P`, `4/4` tests passing.
- 2026-03-19: Re-ran `make audit-fault-connectors audit-flow-loop-shape audit-flow-loop-connectors` after batched-send conversion; results remain stable: 5 known before-save platform fault-connector exceptions, loop-shape `Lookup count: 0` / `DML/action count: 0`, and no `nextValue -> subflow` review candidates.
- 2026-03-19: Deployed `BLL_Onboarding_SFL_Dispatch_Communication_By_Context` + `CommunicationTemplateBulkSendAction` to `OnboardV2` (`0AfRL00000dRSmX0AW`) with `RunSpecifiedTests` on `CommunicationTemplateBulkSendActionTest` (`4/4` passing).
- 2026-03-19: User direction applied: `DOMAIN_OmniSObject_SFL_Send_Email_Communication` hardening is deferred and out of current remediation scope; queued for a later pass.
- 2026-03-19: Started record-triggered entry-criteria tightening (`Only when updated to meet condition`) on allowed `BLL_*_Logical_Process` scope; initial dry-run failed because Salesforce disallows `doesRequireRecordChangedToMeetCriteria=true` when start filters already use `IsChanged` (`BLL_Agreement_RCD_Logical_Process`, `BLL_Onboarding_Requirement_Subject_RCD_Logical_Process`).
- 2026-03-19: Reverted that conflicting flag on the two `IsChanged` start-filter flows, kept `doesRequireRecordChangedToMeetCriteria=true` on `BLL_Order_RCD_Business_Logic`, and revalidated successfully with targeted dry-run deploy (`0AfRL00000dRTCM0A4`).
- 2026-03-19: Deployed the scoped entry-criteria update bundle to `OnboardV2` (`0AfRL00000dRN8Y0AW`): `BLL_Order_RCD_Business_Logic` changed, `BLL_Agreement_RCD_Logical_Process` and `BLL_Onboarding_Requirement_Subject_RCD_Logical_Process` unchanged/validated.
- 2026-03-19: Post-change audit rerun (`make audit-fault-connectors audit-flow-loop-shape audit-flow-loop-connectors`) remains stable at 5 known before-save platform fault-connector exceptions, with loop-shape `Lookup count: 0` and `DML/action count: 0`.
- 2026-03-19: Continued record-triggered entry hardening on the next allowed flow: `BLL_External_Contact_Credential_RCD_Logical_Process` start criteria now includes `POE_Process_Status__c = Training Sent` and uses `doesRequireRecordChangedToMeetCriteria=true` together with existing guards (`Training_Sent_Date__c` null, `POE_Program__c = Verizon Fios D2D`) to avoid repeated no-op updates.
- 2026-03-19: Targeted dry-run validation for `BLL_External_Contact_Credential_RCD_Logical_Process` succeeded (`0AfRL00000dRV7h0AG`) and deployment to `OnboardV2` succeeded (`0AfRL00000dRV9J0AW`).
- 2026-03-19: Post-deploy audits remained stable (`make audit-fault-connectors audit-flow-loop-shape audit-flow-loop-connectors`): 5 known before-save platform fault-connector exceptions only, loop-shape `Lookup count: 0`, `DML/action count: 0`, and no risky `nextValue -> query/DML/action` connectors.
- 2026-03-19: Hardened `BLL_Onboarding_Requirement_RCD_Logical_Process` in place (no create/update split into new flows): start connector now routes to `Is_Create_Event` first so update interviews can skip create-only retrieval work, create branch now enters the existing retrieval chain (`Get_Onboarding_Record` -> `Get_Opportunity_Record` -> `Get_Opportunity_Contact` -> `Get_Account_Record` -> `Get_Vendor_Program`) before approval/related-record handling, and the post-normalization evaluator input assignment now reads `OnboardingId` from `$Record.Onboarding__c` instead of `OnboardingRecord.Id`.
- 2026-03-19: Targeted dry-run validation for `BLL_Onboarding_Requirement_RCD_Logical_Process` succeeded (`0AfRL00000dROpO0AW`) and deployment to `OnboardV2` succeeded (`0AfRL00000dRVxJ0AW`).
- 2026-03-19: Post-deploy audits after onboarding-requirement flow hardening remained stable (`make audit-fault-connectors audit-flow-loop-shape audit-flow-loop-connectors`): same 5 known before-save fault-connector platform exceptions, loop-shape `Lookup count: 0` and `DML/action count: 0`, no risky `nextValue -> query/DML/action` connectors.
- 2026-03-19: Hardened `BLL_Onboarding_RCD_Logical_Process` in place (no flow split): moved `Determine_Execution_Path` to trigger entry, added assignment gates (`Set_Execution_Path_Create`, `Set_Execution_Path_Communication_Update`) so only relevant branches call `Get_Vendor_Program`, and added `Route_After_Vendor_Program` to dispatch post-lookup behavior (`Create` -> `Get_Vendor_Program_Requirements`, `CommunicationUpdate` -> `Determine_Send_Training`), eliminating vendor-program retrieval work on no-op updates.
- 2026-03-19: Targeted dry-run validation for `BLL_Onboarding_RCD_Logical_Process` succeeded (`0AfRL00000dRWN70AO`) and deployment to `OnboardV2` succeeded (`0AfRL00000dRQ9h0AG`).
- 2026-03-19: Post-deploy audits after onboarding logical-process hardening remained stable (`make audit-fault-connectors audit-flow-loop-shape audit-flow-loop-connectors`): same 5 known before-save platform fault-connector exceptions, loop-shape `Lookup count: 0`, `DML/action count: 0`, and no risky `nextValue -> query/DML/action` connectors.
- 2026-03-19: Continued high-volume trigger hardening on `BLL_OmniSObject_RCD_SYNC_Training_Assignments` without flow split: added entry decision `Should_Process_Training_Assignment_Change` (`create OR Status__c changed OR Contact__c changed OR Onboarding__c changed`) and routed start through this gate before `Get_Onboarding_Records` to skip heavy sync/evaluation work on irrelevant updates.
- 2026-03-19: Targeted dry-run validation for `BLL_OmniSObject_RCD_SYNC_Training_Assignments` succeeded (`0AfRL00000dRWbd0AG`) and deployment to `OnboardV2` succeeded (`0AfRL00000dRWgT0AW`).
- 2026-03-19: Post-deploy audits after training-assignment sync hardening remained stable (`make audit-fault-connectors audit-flow-loop-shape audit-flow-loop-connectors`): same 5 known before-save fault-connector platform exceptions, loop-shape `Lookup count: 0`, `DML/action count: 0`, and no risky `nextValue -> query/DML/action` connectors.
- 2026-03-19: Added new bulk invocable `OnbReqParentBulkEvalInvocable` (shortened from an overlong original class name) plus `OnbReqParentBulkEvalInvocableTest`, and rewired `BLL_OmniSObject_RCD_SYNC_Training_Assignments` to replace `Loop_Changed_Onboarding_Requirement_Ids` + per-id `DOMAIN_OmniSObject_SFL_EVAL_Onb_Req_Subjects_Parent` calls with single action call `Evaluate_Changed_Onboarding_Requirements_Bulk`.
- 2026-03-19: Initial deploy/test iterations exposed org-specific constraints: `Onboarding_Requirement__c` status/requirement-type values are record-type-restricted, and `Onboarding_Requirement_Subject__c` enforces exactly one of `Account__c`/`Contact__c`; test fixtures were updated to be org-valid and constraint-aware.
- 2026-03-19: Hardened `OnbReqParentBulkEvalInvocable` update behavior for restricted status writes by adding ordered fallback status attempts and `Completed__c`-only fallback when status cannot be written for the record type, preventing bulk parent-rollup faults on picklist restrictions.
- 2026-03-19: Targeted deploy with specified tests succeeded for `OnbReqParentBulkEvalInvocable` + test + `BLL_OmniSObject_RCD_SYNC_Training_Assignments` (`0AfRL00000dRZsr0AG`, `5/5` passing).
- 2026-03-19: Post-refactor loop audits remained clean (`make audit-flow-loop-shape audit-flow-loop-connectors`): loop-dependent lookup count `0`, loop-dependent DML/action count `0`, and no risky `nextValue -> query/DML/action` connector findings.
- 2026-03-19: Assessed "single flow + CMDT" onboarding-status objective against current metadata and documented remaining architecture gaps: evaluator invocable currently handles only `requests[0]`, `BLL_Order_RCD_Business_Logic` still performs a direct onboarding-status write bypass, and parent-rollup ownership remains split across Flow and Apex paths.
- 2026-03-19: Added business/admin-facing onboarding status governance guide `docs/user-guides/onboarding-status-business-rules-guide.md` covering the evidence -> subject -> requirement -> onboarding chain and CMDT responsibilities.
- 2026-03-19: Added implementation plan `docs/implementation-notes/onboarding-status-single-owner-flow-plan-2026-03-19.md` with P0/P1/P2 steps to finalize single-owner, bulk-safe, CMDT-driven onboarding status evaluation.
- 2026-03-19: Refreshed `docs/processes/status-evaluation.md` and linked the new business guide from `docs/user-guides/onboarding-workflow.md` so business and technical audiences have separate operating documentation.
- 2026-03-19: Corrected `docs/user-guides/onboarding-workflow.md` to remove legacy/incorrect start/process references and align the guide with current OnboardV2 Lightning-page + CMDT workflow.
- 2026-03-19: Implemented evaluator bulk safety in Apex: `OnboardingStatusEvaluatorInvocable` now processes all request rows (not `requests[0]`) and supports collection input `onboardingIds`; `OnboardingStatusEvaluatorService` now exposes bulk `evaluateBulk` / `evaluateAndApplyBulk` paths.
- 2026-03-19: Removed direct onboarding-status bypass from `BLL_Order_RCD_Business_Logic`; flow now collects onboarding ids and invokes `OnboardingStatusEvaluatorInvocable` bulk action (`applyUpdates=true`) instead of directly assigning `Onboarding__c.Onboarding_Status__c = Setup Complete`.
- 2026-03-19: Converged parent rollup callers to the bulk Apex engine: `BLL_Onboarding_Requirement_Subject_RCD_Logical_Process` and `DOMAIN_OmniSObject_SFL_EVAL_Onb_Req_Subjects_For_Onb_Rec` now call `OnbReqParentBulkEvalInvocable` bulk action rather than looped/per-id `DOMAIN_OmniSObject_SFL_EVAL_Onb_Req_Subjects_Parent` subflow calls.
- 2026-03-19: Targeted validation/deploy for this P0 bundle succeeded: dry-run `0AfRL00000dRf8f0AC` and deploy `0AfRL00000dRee20AC` with `OnboardingStatusEvaluatorTest` passing `10/10`.
- 2026-03-19: Post-deploy governance audit rerun (`make audit-flow-loop-shape audit-flow-loop-connectors`) remained clean: loop-dependent lookup count `0`, loop-dependent DML/action count `0`, and no risky `nextValue -> query/DML/action` connectors.
- 2026-03-19: Fixed auto-layout blocker in `DOMAIN_OmniSObject_SFL_EVAL_Onb_Req_Subjects_For_Onb_Rec`: `Assign_Evaluation_Contact_Id_From_Primary_Contact` had no incoming connector after prior refactor; `Did_Get_Primary_Opportunity_Contact_Fail` default path now routes to that assignment, eliminating disconnected element errors for auto-layout conversion. Deployed to `OnboardV2` (`0AfRL00000dRgG10AK`).
- 2026-03-19: Added onboarding-status single-owner guardrail automation: new script `scripts/automation/audit-onboarding-status-owner-writes.sh` and `make audit-onboarding-status-owner` target detect direct `Onboarding__c.Onboarding_Status__c` writes in flows/non-test Apex outside `OnboardingStatusEvaluatorService`.
- 2026-03-19: Executed hardening audit bundle (`make audit-onboarding-status-owner audit-fault-connectors audit-flow-loop-shape audit-flow-loop-connectors`): owner-write violations `0`, fault-connector gaps remain only the 5 known before-save platform exceptions, loop-shape `Lookup count: 0` / `DML/action count: 0`, and no risky loop connector targets.
- 2026-03-19: Attempted direct subflow `faultConnector` hardening in `BLL_Agreement_RCD_Logical_Process`; deployment parser rejected `FlowSubflow.faultConnector` (`0AfRL00000dRgnt0AC`), confirming this metadata shape is unsupported for record-triggered subflow elements.
- 2026-03-19: Pivoted `BLL_Agreement_RCD_Logical_Process` to supported subflow error-contract hardening: `Get_Onboarding_Record` and `Evaluate_Agreement_Subjects` now map `errorMessage` outputs, branch through explicit failure decisions, and route failures to `DOMAIN_OmniSObject_SFL_CREATE_Fault_Message`.
- 2026-03-19: Validated and deployed the agreement hardening update: dry-run `0AfRL00000dRZWI0A4`, deploy `0AfRL00000dRhK90AK`.
- 2026-03-19: Re-ran governance audits post-deploy (`make audit-onboarding-status-owner audit-fault-connectors audit-flow-loop-shape audit-flow-loop-connectors`); results remained stable with owner-write violations `0`, only 5 known before-save fault-connector exceptions, and clean loop-shape/connector risk output.
- 2026-03-19: Added supported-contract governance script `scripts/automation/audit-subflow-error-contracts.sh` and `make audit-subflow-error-contracts` target to detect parent subflow calls that omit `errorMessage` output mapping when the called flow exposes `errorMessage` as output.
- 2026-03-19: Executed `make audit-subflow-error-contracts`; baseline reports `0` missing `errorMessage` output mappings (`.analysis/automation-audit/subflow_error_contract_audit.tsv`).
- 2026-03-19: Final hardening-audit rerun (`make audit-onboarding-status-owner audit-subflow-error-contracts audit-fault-connectors audit-flow-loop-shape audit-flow-loop-connectors`) remained stable: owner-write violations `0`, subflow-contract violations `0`, fault gaps only the 5 known before-save exceptions, and loop-risk audits clear.
- 2026-03-19: Enhanced shared fault handler contract in `DOMAIN_OmniSObject_SFL_CREATE_Fault_Message`: added optional inputs `SourceElementLabel` and `InterviewGuid`, expanded admin fault payload (`ResolvedAdminFaultMessageFormula`) to include both, and changed user output to `ResolvedUserFacingMessageWithReferenceFormula` so users receive a safe message plus short reference token.
- 2026-03-19: Updated `EXP_Opportunity_SCR_Create_Record` handled fault branches to use user-safe fault output: all six `Handle_*_Fault` subflows now map `UserFacingMessage` (instead of `ResolvedFaultMessage`), pass `SourceElementLabel` + `$Flow.InterviewGuid`, and set branch-specific `UserFacingMessageOverride` text.
- 2026-03-19: Validated/deployed user-visible fault messaging bundle (`DOMAIN_OmniSObject_SFL_CREATE_Fault_Message`, `EXP_Opportunity_SCR_Create_Record`): dry-run `0AfRL00000dRbl00AC`, deploy `0AfRL00000dRiGD0A0`.
- 2026-03-19: Post-deploy governance rerun (`make audit-onboarding-status-owner audit-subflow-error-contracts audit-fault-connectors audit-flow-loop-shape audit-flow-loop-connectors`) remained stable: owner-write violations `0`, subflow contract violations `0`, only 5 known before-save fault-connector exceptions, no loop-risk findings.
- 2026-03-19: Hardened `EXP_Opportunity_SCR_Create_Record` `ResolvedFlowErrorMessage` formula to prioritize handler-returned user-safe `errorMessage` over raw `QueueChainMessage`, ensuring handled faults do not leak internal queue text to users.
- 2026-03-19: Validated/deployed the formula follow-up on `EXP_Opportunity_SCR_Create_Record`: dry-run `0AfRL00000dRPaB0AW`, deploy `0AfRL00000dRiPt0AK`.
- 2026-03-19: Governance rerun after formula hardening (`make audit-onboarding-status-owner audit-subflow-error-contracts audit-fault-connectors audit-flow-loop-shape audit-flow-loop-connectors`) remained stable with the same baseline (`0` owner-write, `0` subflow-contract, 5 known before-save fault exceptions, no loop risks).
- 2026-03-19: Extended user-safe fault handling to `EXP_Contract_SCR_Send_Adobe_Agreement`: rerouted `Adobe_Agreement_Created` default (no agreement returned) through shared fault logger, replaced direct `Capture_Update_Contract_Fault -> Screenflow2aError` with `Handle_Update_Contract_Record_Fault` logger path, and updated `Handle_Update_Onboarding_Record_Fault` to map `UserFacingMessage` output.
- 2026-03-19: Added context fields on all three contract flow handlers (`SourceElementLabel`, `$Flow.InterviewGuid`, branch-specific `UserFacingMessageOverride`) and updated `Screenflow2aError` / `Onboarding_Update_Error` display text to render `{!errorMessage}` so users see safe reference-bearing messages.
- 2026-03-19: Validated/deployed contract flow user-fault hardening: dry-run `0AfRL00000dRijF0AS`, deploy `0AfRL00000dRio50AC`.
- 2026-03-19: Post-deploy governance rerun (`make audit-onboarding-status-owner audit-subflow-error-contracts audit-fault-connectors audit-flow-loop-shape audit-flow-loop-connectors`) remained stable: owner-write `0`, subflow-contract `0`, only 5 known before-save fault exceptions, no loop-risk findings.
- 2026-03-19: Added screen-flow fault UX contract automation: new script `scripts/automation/audit-screen-flow-fault-user-messaging.sh` and `make audit-screen-flow-fault-user-messaging` target enforce that Screen Flows calling `DOMAIN_OmniSObject_SFL_CREATE_Fault_Message` map `UserFacingMessage` and pass `InterviewGuid` + `SourceElementLabel`.
- 2026-03-19: Full governance rerun including screen-flow UX contract (`make audit-onboarding-status-owner audit-subflow-error-contracts audit-screen-flow-fault-user-messaging audit-fault-connectors audit-flow-loop-shape audit-flow-loop-connectors`) remained stable: owner-write `0`, subflow-contract `0`, screen-flow UX contract `0`, fault gaps unchanged to the 5 known before-save exceptions, and no loop-risk findings.
- 2026-03-19: Completed Step 1 fault-context standardization pass on active non-deferred fault handlers by adding `SourceElementLabel` + `$Flow.InterviewGuid` inputs to all `DOMAIN_OmniSObject_SFL_CREATE_Fault_Message` subflow calls in 12 flows (`BLL_*` and `DOMAIN_OmniSObject_*` in-scope). `DOMAIN_OmniSObject_SFL_Send_Email_Communication` remained unchanged per user defer decision.
- 2026-03-19: Validated and deployed the Step 1 flow bundle to `OnboardV2`: dry-run `0AfRL00000dRov70AC`, deploy `0AfRL00000dRozx0AC` (12/12 flows changed).
- 2026-03-19: Post-deploy governance checks stayed clean for this step: `make audit-subflow-error-contracts` (`0` violations) and `make audit-screen-flow-fault-user-messaging` (`0` violations).
- 2026-03-19: Implemented Step 2 guardrail automation `scripts/automation/audit-fault-message-context-contract.sh` and `make audit-fault-message-context-contract` to enforce required shared-fault context inputs (`SourceElementApiName`, `SourceElementLabel`, `SourceFlowApiName`, `InterviewGuid`) on every `DOMAIN_OmniSObject_SFL_CREATE_Fault_Message` call across in-scope flows.
- 2026-03-19: Implemented Step 3 screen-flow contract tightening by enhancing `scripts/automation/audit-screen-flow-fault-user-messaging.sh` to also enforce `SourceElementApiName` + `SourceFlowApiName` and flag any `ResolvedFaultMessage` output mappings on screen-flow fault handlers.
- 2026-03-19: Completed Step 4 validation run for these guardrails: `make audit-fault-message-context-contract` reports `0` violations and `make audit-screen-flow-fault-user-messaging` reports `0` violations.
- 2026-03-19: Pivoted from audit-only hardening to deployable repo changes on the Record Collection Editor path: updated `RecordCollectionEditorConfigService` to use `Database.insert/update(..., AccessLevel.USER_MODE)` for child + relationship writes, added relationship field-level create/update gates, and added `WITH SECURITY_ENFORCED` to relationship existence lookup.
- 2026-03-19: Fixed USER_MODE relationship update behavior by switching update payloads to Id-only stubs (avoids write attempts to inaccessible non-updateable key fields such as `AccountId` / `ContactId` on `AccountContactRelation`).
- 2026-03-19: Hardened `RecordCollectionEditorGetRecordsService` JSON serializer to safely read optionally-unqueried fields (guarded `record.get(field)` access), added `WITH SECURITY_ENFORCED` to fallback relationship query path, and replaced raw exception passthrough with user-safe reference-based error output + `LoggingUtil` detailed logging.
- 2026-03-19: Validation/deploy results for this deployable hardening bundle: targeted dry-run with specified tests `0AfRL00000dRpSz0AK` (`4/4` passing; coverage-warning-only deploy gate on specified-test mode), compile dry-runs `0AfRL00000dRpeH0AS` and `0AfRL00000dRpfu0AC` succeeded, deployments to `OnboardV2` succeeded `0AfRL00000dRlAs0AK` (config + get-records baseline) and `0AfRL00000dRqC90AK` (get-records user-safe error follow-up).
- 2026-03-19: Post-deploy org verification: `RecordCollectionEditorConfigServiceTest` run `707RL00001Ih0Ym` passed `4/4`.
- 2026-03-19: Added deployable SOQL security hardening to `ExperienceOpportunityCreateAsyncService`: both vendor-program lookup SOQL branches in `resolveVendorProgramForCreate` now use `WITH SECURITY_ENFORCED`.
- 2026-03-19: Validation/deploy for `ExperienceOpportunityCreateAsyncService` hardening: targeted dry-run with specified tests `0AfRL00000dRqFN0A0` (`3/3` passing; coverage-warning-only deploy gate on specified-test mode), compile dry-run `0AfRL00000dRqGz0AK` succeeded, deployment succeeded `0AfRL00000dRpj80AC`.
- 2026-03-19: Post-deploy org verification: `ExpOppCreateAsyncServiceTest` run `707RL00001Ih0f8` passed `3/3`.
- 2026-03-20: Follow-up deployable security hardening on `RecordCollectionEditorConfigService`: `getConfig` now filters inaccessible fields out of both child and relationship field config payloads before returning editor metadata.
- 2026-03-20: Validation/deploy for the `getConfig` inaccessible-field filter update: compile dry-run `0AfRL00000dSGZd0AO` succeeded, deployment succeeded `0AfRL00000dSGcr0AG`.
- 2026-03-20: Post-deploy org verification: `RecordCollectionEditorConfigServiceTest` run `707RL00001IjQqI` passed `4/4`.
- 2026-03-20: Additional deployable read-path hardening on `RecordCollectionEditorGetRecordsService`: configured child/relationship field API names are now filtered by describe accessibility before extraction/query, reducing runtime FLS/query failures on drifted metadata.
- 2026-03-20: Validation/deploy for the read-path filter update: compile dry-run `0AfRL00000dSGxp0AG` succeeded, deployment succeeded `0AfRL00000dSH2f0AG`.
- 2026-03-20: Post-deploy org verification: `RecordCollectionEditorConfigServiceTest` run `707RL00001IjW2a` passed `4/4`.
- 2026-03-20: Hardened `ObjectRelatedListController` dynamic query guards to reduce injection/drift risk while preserving existing LWC behavior: added describe-validated field/path checks (`requireSimpleField`, `requireFieldPath`), replaced direct object-name interpolation with describe-derived object name, and validated lookup label/sort fields before query assembly.
- 2026-03-20: Added targeted controller regression coverage `ObjectRelatedListCtrlTest` (related-record query with relationship fields, invalid dynamic input rejection, lookup options behavior, and guard-path validation).
- 2026-03-20: Validation/deploy for `ObjectRelatedListController` + `ObjectRelatedListCtrlTest`: dry-run `0AfRL00000dSJio0AG` succeeded and deployment to `OnboardV2` succeeded `0AfRL00000dSSNt0AO` with `5/5` specified tests passing.
- 2026-03-20: User-reported regression found in Lightning App Builder for `Onboarding Requirement History`: config using `Parent Lookup Field API Name = Parent.Onboarding__c` began failing with `Invalid Parent field` after strict parent-field hardening.
- 2026-03-20: Applied compatibility fix in `ObjectRelatedListController`: parent filter validation now uses relationship-path validation (`requireFieldPath`) instead of simple-field-only validation, restoring supported cross-object parent filters while retaining injection-safe path checks.
- 2026-03-20: Added regression test `ObjectRelatedListCtrlTest.testGetRelatedRecordsWithParentRelationshipPath` and validated/deployed fix bundle to `OnboardV2` (`dry-run 0AfRL00000dSSe10AG`, deploy `0AfRL00000dSP0F0AW`, `6/6` specified tests passing).
- 2026-03-20: Additional user-reported compatibility regression confirmed for existing App Builder configs that place relationship paths in `Fields (CSV)` (example `CreatedBy.Name`): strict requested-field validation was still enforcing simple fields only.
- 2026-03-20: Completed dotted-path compatibility across all query-field inputs in `ObjectRelatedListController`: `fieldApiNames` now accepts validated relationship paths (same path validator used for parent/order/relationship fields), restoring prior flexible configuration behavior while preserving describe-based safety checks.
- 2026-03-20: Added regression test `ObjectRelatedListCtrlTest.testGetRelatedRecordsWithRelationshipPathInFieldApiNames` and validated/deployed compatibility bundle to `OnboardV2` (`dry-run 0AfRL00000dSSXb0AO`, deploy `0AfRL00000dSO7T0AW`, `7/7` specified tests passing).
- 2026-03-20: Verified dotted-path runtime compatibility directly in `OnboardV2` via anonymous Apex using `ObjectRelatedListController.getRelatedRecords` with `objectApiName = Onboarding_Requirement__History`, `parentFieldApiName = Parent.Onboarding__c`, and dotted field inputs (`CreatedBy.Name`, `Parent.Requirement_Type__c`); execution returned success (`ORL_VERIFY_SUCCESS`).
- 2026-03-20: Added LWC Jest coverage for `objectRelatedList` dotted-path behavior with new suite `force-app/test/lwc/objectRelatedList.test.js` (wire-config contract, parent-id source path, dotted exclusion/transform rendering).
- 2026-03-20: Extended local Jest harness with missing module shims required by this component tree (`lightning/primitiveIcon`, `lightning/actions`, `lightning/refresh`) via `force-app/test/jest-mocks/lightning/*` and `jest.config.js` `moduleNameMapper`.
- 2026-03-20: Executed targeted LWC unit tests: `npx sfdx-lwc-jest -- --runTestsByPath force-app/test/lwc/objectRelatedList.test.js` passed (`3/3`).
- 2026-03-20: Expanded `objectRelatedList` Jest suite with picklist-by-recordType resilience coverage to lock the prior incident paths: (1) recordType success hydration, (2) UI API wire-error fallback to object-info picklist values, and (3) queue progression across two record types without deadlock.
- 2026-03-20: Re-ran targeted and full LWC test execution after picklist additions: `npx sfdx-lwc-jest -- --runTestsByPath force-app/test/lwc/objectRelatedList.test.js` passed (`6/6`), and `npx sfdx-lwc-jest` baseline passed (`6/6`).
- 2026-03-20: Deployability check surfaced a packaging blocker when Jest files were inside the LWC bundle path (`__tests__` under `force-app/main/default/lwc/objectRelatedList`): dry-run deploy `0AfRL00000dSJe10AG` failed with `LWC1503` wire-adapter compile errors because Salesforce attempted to compile test harness imports.
- 2026-03-20: Resolved deployability by relocating Jest suite to repo-only path `force-app/test/lwc/objectRelatedList.test.js` (outside metadata bundle), then re-validated deployment safety: dry-run `0AfRL00000dSNxi0AG` succeeded and deploy `0AfRL00000dSWbB0AW` succeeded.
- 2026-03-20: Added DOM/event-driven Jest regression suite `force-app/test/lwc/programDatesQuickAction.test.js` to cover vendor option mapping, submit defaults (`Account__c`/`Vendor_Program__c`), submit path spinner state, and success/error toast + close behaviors without relying on non-`@api` internals.
- 2026-03-20: Added Jest regression suite `force-app/test/lwc/programDatesRelatedList.test.js` to cover wire row transforms, wire error rendering, inline save success/error, and row delete action behavior.
- 2026-03-20: Validated new `programDates*` suites with targeted run `npx sfdx-lwc-jest -- --runTestsByPath force-app/test/lwc/programDatesRelatedList.test.js force-app/test/lwc/programDatesQuickAction.test.js` (`11/11` passing).
- 2026-03-20: Re-ran full LWC Jest baseline after adding `programDates*` suites: `npx sfdx-lwc-jest` now passes `17/17`.
- 2026-03-20: Deployed `programDatesQuickAction` + `programDatesRelatedList` LWC bundles to `OnboardV2` after dry-run validation; deploy succeeded (`0AfRL00000dSVyU0AW`).
- 2026-03-20: Applied targeted LWC accessibility hardening on active related-list surfaces: replaced `javascript:void` anchor actions with keyboard-native button controls in `objectRelatedList` and `programDatesRelatedList`, added `aria-live` status regions plus `role="alert"` error regions, and bound `programDatesQuickAction` vendor combobox to native `label` for explicit form-control association.
- 2026-03-20: Expanded Jest coverage to lock accessibility behavior (`objectRelatedList`, `programDatesRelatedList`, `programDatesQuickAction`) and validated with targeted run `npx sfdx-lwc-jest -- --runTestsByPath force-app/test/lwc/objectRelatedList.test.js force-app/test/lwc/programDatesRelatedList.test.js force-app/test/lwc/programDatesQuickAction.test.js` (`19/19` passing), then re-ran full baseline `npx sfdx-lwc-jest` (`19/19` passing).
- 2026-03-20: Deployed accessibility hardening bundle (`objectRelatedList`, `programDatesRelatedList`, `programDatesQuickAction`) to `OnboardV2`; deploy succeeded (`0AfRL00000dSXyg0AG`).
- 2026-03-20: Implemented LWC performance hardening for related-list refresh paths: `objectRelatedList` and `programDatesRelatedList` now debounce manual refresh clicks (`200ms`) and coalesce concurrent `refreshApex` calls; `objectRelatedList` now suppresses self-induced duplicate refresh loops when dispatching `RefreshEvent`.
- 2026-03-20: Optimized picklist record-type loading in `objectRelatedList`: default record type picklist fetch is now requested only when rows are missing `RecordTypeId` (or no record type ids are present), avoiding unnecessary record-type UI API calls on large lists where all rows already carry explicit record types.
- 2026-03-20: Expanded Jest performance coverage with debounce tests for `objectRelatedList` and `programDatesRelatedList`, then validated with targeted run `npx sfdx-lwc-jest -- --runTestsByPath force-app/test/lwc/objectRelatedList.test.js force-app/test/lwc/programDatesRelatedList.test.js force-app/test/lwc/programDatesQuickAction.test.js` (`21/21` passing) and full baseline `npx sfdx-lwc-jest` (`21/21` passing).
- 2026-03-20: Deployed LWC performance hardening bundle (`objectRelatedList`, `programDatesRelatedList`) to `OnboardV2`; deploy succeeded (`0AfRL00000dST750AG`).
- 2026-03-20: Implemented GraphQL read-path proof of concept in `objectRelatedList` using `lightning/uiGraphQLApi` wire adapter (`graphql` + `refreshGraphQL`) behind new feature flag `useGraphqlReadPath`; PoC is intentionally scoped to supported shape (`Program_Dates__c` child + `Account__c` parent filter + DESC/default sort), with automatic Apex wire fallback for unsupported configs.
- 2026-03-20: Added PoC guard regression in `force-app/test/lwc/objectRelatedList.test.js` to verify Apex wire config is suppressed when GraphQL flag + supported shape are active; revalidated targeted Jest run (`22/22`) and full baseline (`22/22`).
- 2026-03-20: Validated GraphQL PoC metadata with dry-run deploy `0AfRL00000dSbe50AC` and deployed `objectRelatedList` PoC bundle to `OnboardV2` via `0AfRL00000dSY1v0AG`.
