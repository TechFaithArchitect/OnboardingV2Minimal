<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Require_Exactly_One_Subject</fullName>
    <active>true</active>
    <description>Requires exactly one subject target so each row represents either an account or a contact.</description>
    <errorConditionFormula>OR(
    AND(
        ISBLANK(Account__c),
        ISBLANK(Contact__c)
    ),
    AND(
        NOT(ISBLANK(Account__c)),
        NOT(ISBLANK(Contact__c))
    )
)</errorConditionFormula>
    <errorDisplayField>Account__c</errorDisplayField>
    <errorMessage>Populate either Account or Contact, but not both.</errorMessage>
</ValidationRule>
