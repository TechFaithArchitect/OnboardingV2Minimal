# Salesforce CLI local cache - can contain stale paths causing UnsafeFilepathError
.sf/
.sfdx/

# Local org auth
.localdevserver/

# One-off Production Field Service metadata snapshots (not deployed from this repo)
retrieve-prod-fsl-snapshot/
OnboardingV2Minimal/docs